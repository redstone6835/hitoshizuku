[package]
name = "hal"
version = "0.1.0"
edition = "2024"

[lib]
test = false
doctest = false
bench = false

[dependencies]
arch = { path = "../arch" }
elf = { path = "../libs/elf" }
general = { path = "../general" }
kernel-symbols = { path = "../libs/kernel-symbols" }
