[package]
name = "socket"
version = "0.1.0"
edition = "2024"

[lib]
test = true
doctest = false
bench = false

[dependencies]
kernel-symbols = { path = "../kernel-symbols" }
sched = { path = "../sched" }

[dev-dependencies]
ktest = { path = "../ktest" }
