[package]
name = "socket"
version = "0.1.0"
edition = "2024"

[lib]
test = true
doctest = false
bench = false

[features]
performance-profile = ["sched/performance-profile"]

[dependencies]
sched = { path = "../sched" }

[dev-dependencies]
ktest = { path = "../ktest" }
