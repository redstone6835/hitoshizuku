[package]
name = "elf"
version = "0.1.0"
edition = "2024"

[lib]
test = true
doctest = false
bench = false

[dependencies]
errno = { path = "../errno" }
log = { path = "../log" }
mm = { path = "../mm" }

[dev-dependencies]
ktest = { path = "../ktest" }
