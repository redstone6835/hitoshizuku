[package]
name = "elf"
version = "0.1.0"
edition = "2024"

[lib]
test = true
doctest = false
bench = false

[features]
ktest-kernel = ["ktest/kernel"]

[dependencies]
errno = { path = "../errno" }
log = { path = "../log" }
mm = { path = "../mm" }
ktest = { path = "../ktest", optional = true }

[dev-dependencies]
ktest = { path = "../ktest" }
