[package]
name = "mm"
version = "0.1.0"
edition = "2024"

[lib]
test = true
doctest = false
bench = false

[dependencies]
errno = { path = "../errno" }
log = { path = "../log" }

[dev-dependencies]
ktest = { path = "../ktest" }
