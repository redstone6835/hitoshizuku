[package]
name = "extfs"
version = "0.1.0"
edition = "2024"

[lib]
test = false
doctest = false
bench = false

[dependencies]
vfs = { path = "../vfs" }
errno = { path = "../errno" }
log = { path = "../log" }
spin = { version = "0.9", default-features = false, features = ["spin_mutex"] }
