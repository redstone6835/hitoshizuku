[package]
name = "net"
version = "0.1.0"
edition = "2024"

[lib]
test = true
doctest = false
bench = false

[dependencies]
spin = { version = "0.9", default-features = false, features = ["spin_mutex", "rwlock"] }
