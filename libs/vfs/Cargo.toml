[package]
name = "vfs"
version = "0.1.0"
edition = "2024"

[lib]
test = true
doctest = false
bench = false

[dependencies]
errno = { path = "../errno" }
mm = { path = "../mm" }
sched = { path = "../sched" }
socket = { path = "../socket" }

[dev-dependencies]
ktest = { path = "../ktest" }
