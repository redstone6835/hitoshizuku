#import "../config.typ": project-name
#import "../styles/diagram.typ": flow-arrow, flow-node, layer-card
#import "../styles/figure.typ": continued-table, figure-caption, pseudo-sample
#import "../styles/tokens.typ": handoff-fill, soft-fill, stable-fill, warm-fill

= 第一章 分层结构与启动控制流

众所周知，在目前，市场上并存着多种指令集与架构，而每种架构又衍生出各种繁多的硬件平台。于是，跨平台的兼容性与可维护性，便无形中成为了操作系统内核开发的首个核心命题。要化解这一难题，一种最可行的思路，是将整个项目划分为两大领域：其一是与具体架构（或平台）紧密相关的底层处理逻辑，其二是摆脱了架构束缚的高层通用逻辑。

这两层之间，需要一个信物来完成权力的交接。我们知道，虽然市面上并存着大量不同的平台，但它们的逻辑却万变不离其宗。平台的作用无非是给操作系统传递所需要的硬件信息以及操作接口，虽然信息如同各国语言一样看起来不同，但它仍然是信息；接口的形式虽然不同，但它终归是接口。因此，我们自然想到可以构造一个特殊的大一统的数据结构（称为"启动上下文"），来传递平台想要给我们操作系统传递的一切信息，只不过我们去除掉了平台不同所带来的信息与接口在表象上的差异。因此，我们便可以将架构（或平台）相关的底层看作是构造"启动上下文"的过程，而架构无关的高层逻辑则是针对这个数据结构所进行的各种操作。底层代码将硬件的千差万别封缄其中，凝练成一个干净的、可供高层直接消费的交接令，从此，高层逻辑便无需再沾染具体器物的尘埃，只需凭借这一纸奏章，便可运筹帷幄、垂拱而治。

古人云："形而上者谓之道，形而下者谓之器。"平台无关的高层逻辑不依赖于特定指令集，提炼出操作系统最本质的法则与策略，如同天道长存，不为尘世纷繁所动。而平台相关的底层处理则是道的落脚之处，埋首于寄存器、页表、异常入口这些物理世界的不毛之地，将看似杂乱无章的硬件现场梳理成有理可循的秩序。此外，启动过程本身就充满了变数，不同的启动方式、不同的固件协议带来了不同的初始状态，这正如世事之权变，而平台无关的逻辑正是那不变的常经。经权相济，无论面临何种权变，只要底层实现了兼容，就总能将其转化、纳入同一个轨道，使高层始终面对着一套统一、可靠的启动语境。此所谓"以道驭器，以经统权，君逸臣劳，大制不割"。正是这种思想的支撑，我们的操作系统如果要支持一个新的平台，只需要找到一个足够稳定的构造"启动上下文"的方式就可以了，而不需要关心其他任何东西，使得我们的操作系统内核在面对百家争鸣的计算机平台生态时，始终保持着那份"常无欲以观其妙，常有欲以观其徼"的从容与通透。

== 项目分层结构

项目由五个层级构成，从底至上依次为 `libs`、`general`、`arch`、`hal` 和 `kernel`。最底层的 `libs` 提供各种通用逻辑的可复用库，虽然是通用逻辑，但是这些库不依赖上层策略，只暴露最小接口。`general` 位于 `libs` 之上，定义平台无关的通用数据结构、固件解析框架、设备模型和启动上下文类型，形成一套硬件无关的初步抽象层。`arch` 是 `general` 中抽象部分的实现，它实现了那些需要接触具体指令集的接口，包括控制寄存器操作、早期地址映射、汇编入口、异常处理、页表机制和固件表快照等和架构高度耦合的操作。`hal` 对 `arch` 进行封装，将不同架构的实现收敛为统一接口，使更高层不需要关心当前运行在哪一种处理器上。最上层的 `kernel` 是策略与集成层，它只通过 `hal` 和 `general` 获取稳定能力，完成分配器、设备、控制台和后续用户态入口的组织。

#figure(caption: figure-caption("图", "1-1", [项目分层结构]))[
  #layer-card("libs", [可复用算法与驱动。包含独立的库模块，不依赖上层策略，只提供通用能力。], fill: soft-fill)
  #align(center)[#text(fill: rgb("#2d5f73"))[↑]]
  #layer-card("general", [平台无关 trait 与基础设施。定义硬件抽象、固件解析、设备枚举、控制台框架和启动上下文。], fill: soft-fill)
  #align(center)[#text(fill: rgb("#2d5f73"))[↑]]
  #layer-card("arch", [架构实现层。实现 `general` 定义的 trait，处理早期环境、提供不同平台接口的初步封装，并构造启动上下文。], fill: warm-fill)
  #align(center)[#text(fill: rgb("#2d5f73"))[↑]]
  #layer-card("hal", [统一架构接口层。封装 `arch`，将不同架构的实现收敛为统一接口，供 `kernel` 使用。], fill: soft-fill)
  #align(center)[#text(fill: rgb("#2d5f73"))[↑]]
  #layer-card("kernel", [策略与集成层。通过 `hal` 和 `general` 获得稳定能力，编排启动顺序，进行激活 `allocator`、注册设备、初始化用户态内存空间等操作系统初始化操作，是整个操作系统内核的外壳与顶层。], fill: stable-fill)
]

事实上，这五层的关键在于信息流的方向。`arch` 了解具体处理器如何建立早期地址映射、如何操作控制寄存器、如何从启动环境中获取固件参数，`kernel` 不重复这些硬件细节，只关心操作系统具体要做哪些事情，而不用刻意去关心怎么做、在不同平台上应当怎样不同地做。`general` 定义所有架构都能理解的启动上下文和固件解析入口，起到了给 `arch` 设置填空题的作用，而 `arch` 所需要做的就仅仅是根据平台的不同将这些空填上。`libs` 只提供最通用的算法和注入点，例如 `allocator` 接收地址转换函数和堆映射函数，但很显然，中间的算法与平台或者架构等没有任何关系。

== 启动链路概览

*内核启动是一个逐层降低不确定性的过程*。固件将控制权交给内核时，处理器可能处于各种不同的状态，有的平台提供了完整的页表和异常处理，有的平台只提供了最基本的物理内存访问；有的平台通过 ACPI 描述硬件，有的平台通过 DTB 描述硬件。内核启动的任务就是将这些千差万别的初始状态，逐步转化为一个统一的、稳定的运行环境。

整个启动链路可以分为两个大阶段：架构相关启动阶段和平台无关启动阶段。架构相关启动阶段的职责是建立最小的运行环境，收集并稳定化固件信息，构造启动上下文。平台无关启动阶段的职责是解释启动上下文，激活运行期子系统，为用户态和调度器做准备。这两个阶段之间的分界点是启动上下文的传递：架构加载器构造启动上下文并跳转到内核启动初始化，从这一刻开始，控制权进入平台无关层，不再回头访问架构相关的代码。

#figure(caption: figure-caption("图", "1-2", [启动链路概览]))[
  #flow-node([固件将控制权交给内核入口], fill: warm-fill)
  #flow-arrow()
  #flow-node([架构相关启动阶段：建立早期环境、收集固件信息、构造启动上下文], fill: warm-fill)
  #flow-arrow()
  #flow-node([启动上下文传递：架构相关层到平台无关层的唯一接口], fill: handoff-fill)
  #flow-arrow()
  #flow-node([平台无关启动阶段：解析固件表、激活运行期子系统], fill: soft-fill)
  #flow-arrow()
  #flow-node([主入口：用户态初始化、调度器启动], fill: stable-fill)
]

#continued-table(
  "1-1",
  [启动阶段职责划分],
  (1.5fr, 2.5fr),
  (
    table.cell(fill: soft-fill)[#text(weight: "bold")[阶段]],
    table.cell(fill: soft-fill)[#text(weight: "bold")[职责]],
  ),
  (
    table.cell(fill: warm-fill)[架构相关启动],
    table.cell(fill: warm-fill)[建立早期虚拟地址映射、安装异常处理、初始化日志和分配器、收集固件信息、选择固件源、快照固件表、构造启动上下文。],
    table.cell(fill: handoff-fill)[启动上下文传递],
    table.cell(fill: handoff-fill)[将稳定化的启动信息、固件表快照、地址转换函数、分配器回调封装成启动上下文，通过栈上构造和指针传递完成控制权转移。],
    table.cell(fill: soft-fill)[平台无关启动],
    table.cell(fill: soft-fill)[解析固件表、注册电源控制、激活运行期分配器、注册设备和控制台、切换日志输出、为主入口准备运行环境。],
    table.cell(fill: stable-fill)[主入口],
    table.cell(fill: stable-fill)[承接已初始化的内核环境，启动用户态地址空间、调度器、系统调用表和第一个用户进程。],
  ),
  kind: "表",
  continuation-kind: "续表",
  align: (left, left),
)

== 架构相关启动阶段

架构相关启动阶段的核心任务是将固件交给内核的不确定状态，转化为一个可以运行高级语言代码的稳定环境，并将固件提供的硬件描述信息收集、稳定化并封装成启动上下文。这个阶段高度依赖具体的处理器架构和固件规范，不同平台的实现差异巨大，但它们的目标是相同的：为平台无关层提供一个统一的、稳定的启动上下文。

=== 早期环境建立

由于在同一个架构上仍然存在着不同的启动协议，这些启动协议的实现差异仍然巨大，因此我们不得不引入一个兼容层，如对于 EFI Stub 的引导方式，我们提供了一个特殊的最小入口用于从 UEFI 传递的信息中提取同样的初步信息，等等。我们的初步信息以 QEMU 在内核直启条件下的传参规范为准，因此这样的处理方式在多数平台上应该不会出现问题，而 QEMU 内核直启路径本身也成为了这些特殊入口的聚合点，我们称之为统一入口。由于在进入统一入口之前的各种操作仅仅是从不同参数中提取共通信息，这里不再对不同平台的相关操作展开过多赘述，同时我们也将平台的行为看作是直接进入了统一入口，忽略掉前面进行的提取参数的操作。因此，我们的早期环境建立将从统一入口开始讲起。

早期环境的建立包括两个关键步骤：统一入口负责建立最小硬件访问环境，高级语言负责准备建立函数调用所需的栈和调用约定。代码 1-1 展示了这两个步骤的控制流。
#pseudo-sample("1-1", [早期环境建立流程], kind: "代码")[
  ```c
  // 统一入口：建立最小硬件访问环境
  void _start(void) {
      setup_early_address_mapping();    // 配置早期虚拟地址映射
      install_exception_entry();      // 安装异常入口
      clear_control_registers();        // 清理控制寄存器状态
      jump_to_start_virtualized();      // 跳转到高级语言环境准备
  }

  // 高级语言环境准备：建立函数调用环境
  void _start_virtualized(void) {
      clear_stack_boundary();           // 清空返回地址和帧指针
      switch_to_temp_stack();           // 切换到临时栈
      pre_boot_init();                  // 调用预启动初始化
      jump_to_kernel_arch_loader();     // 跳转到内核架构加载器
  }

  // 预启动初始化：清零 BSS 段并保存启动参数
  void pre_boot_init(void) {
      clear_bss_section();              // 清零 BSS 段
      save_boot_params_from_regs();     // 从寄存器保存启动参数
  }
  ```
]

统一入口的编排原理基于最小化假设，即进入入口时不存在可用栈、没有完整页表、没有设备模型，因此只能执行最基本的硬件操作。虚拟地址映射必须最先建立，因为后续所有内存访问都依赖它；异常入口必须紧随其后，因为后续任何意外故障都需要可控的处理路径；控制寄存器清理确保处理器处于已知状态。这三个操作的共同特点是：它们都是纯硬件操作，不依赖任何软件基础设施，可以在极其有限的环境中完成。

高级语言环境准备的编排原理基于调用约定的要求。调用栈边界清理必须最先完成，因为它影响调试器和异常处理程序的行为；临时栈切换必须在调用任何函数之前完成，因为函数调用需要栈；预启动初始化必须在这两者之后，因为它本身就是一个函数调用。

预启动初始化的编排原理基于数据依赖关系。BSS 段清零必须在保存启动参数之前完成，因为启动参数可能保存在 BSS 段的静态变量中，如果顺序颠倒，启动参数会被清零操作覆盖。这是一个典型的写后读依赖（Write-After-Read dependency），必须严格遵守顺序。

=== 内核架构加载器

内核架构加载器是架构相关启动阶段的核心，它的职责是建立早期基础设施、收集固件信息、选择固件源、快照固件表、构造启动上下文。代码 1-2 展示了内核架构加载器的核心算法流程。

#pseudo-sample("1-2", [内核架构加载器核心算法流程], kind: "代码")[
  ```c
  void __kernel_arch_loader(void) {
      // 步骤 1：建立早期基础设施
      install_exception_handlers();
      init_timestamp_source();
      bind_early_log_sink();
      init_boot_allocator();

      // 步骤 2：收集固件信息
      firmware_table = read_firmware_system_table();
      if (firmware_table == NULL) {
          panic("no firmware table available");
      }

      // 步骤 3：选择固件源（ACPI 优先于 DTB）
      if (has_acpi_in_firmware_table(firmware_table)) {
          selected_source = FIRMWARE_SOURCE_ACPI;
      } else if (has_dtb_in_firmware_table(firmware_table)) {
          selected_source = FIRMWARE_SOURCE_DTB;
      } else {
          panic("no valid firmware source");
      }

      // 步骤 4：退出固件服务
      exit_firmware_boot_services();

      // 步骤 5：快照固件表到内核缓冲区
      snapshot_firmware_tables(selected_source);

      // 步骤 6：构造启动上下文并跳转
      context = build_start_context(selected_source);
      jump_to_kernel_start_init(&context);  // 无返回
  }
  ```
]

内核架构加载器的编排原理基于依赖关系和时机约束。

早期基础设施的建立顺序体现了依赖链：异常处理程序是所有后续操作的安全网，必须最先安装；时间戳源是日志系统的依赖，必须在日志绑定前初始化；日志系统是诊断信息的输出通道，必须在启动分配器前可用；启动分配器依赖 BSS 段已清零和日志系统可用。这是一个典型的依赖链（dependency chain），每个环节都依赖前一个环节的输出。

固件源选择的时机约束基于固件服务的生命周期。固件系统表中的配置表条目可能位于固件服务内存中，一旦退出固件服务，这些内存可能被回收。因此，固件源选择必须在退出固件服务之前完成。固件源选择采用单向决策而非回退机制，原因有二：一是 ACPI 提供的描述在大多数场景下更完整，这是工业界的共识；二是如果选择的固件源无法解析，通常意味着固件或硬件本身存在问题，继续启动可能导致更严重的后果。这种"快速失败"（fail-fast）策略有助于及早发现问题。

固件服务退出的时机约束基于资源所有权的转移。退出固件服务是一个不可逆操作，从这一刻开始，固件占用的资源（内存、设备、服务）都可能被回收或失效。因此，退出时机必须在完成所有固件信息收集之后、开始固件表快照之前。当前设计将固件系统表的地址和配置表条目在退出前读取，而固件表的内容在退出后从已知的物理地址复制，这种分离使得内核可以尽早接管系统资源，同时保证信息完整性。

启动上下文的构造时机约束基于数据完整性和生命周期管理。启动上下文必须在所有固件表快照完成之后构造，因为它需要引用这些快照的地址。同时，启动上下文必须在当前栈上构造而非通过分配器分配，因为跳转到内核启动初始化后，当前栈可能被回收。这种"栈上构造、指针传递"的设计使得启动上下文成为真正的一次性对象，避免了生命周期管理的复杂性。

=== 启动上下文

启动上下文是架构相关层和平台无关层之间的唯一接口。它不是长期保存的启动数据库，也不是固件解析模块的公共输入，而是架构加载器到内核启动初始化的一次性交接对象。架构加载器在当前启动栈上构造它，把指针放入调用约定寄存器，然后无返回跳转到内核启动初始化入口。从这一刻开始，控制权进入平台无关层，架构加载器不再参与。

启动上下文中保存的是已经稳定化之后的启动信息，而不是未解释的硬件描述。它包含启动协议、当前架构、启动处理器标识、被选择的固件来源、稳定的固件表视图、内核镜像物理范围、启动内存图、普通物理地址到虚拟地址的转换函数、设备地址转换函数，以及分配器需要的架构回调。关键约束是上下文只描述能力和数据，不把特定架构的常数、寄存器编号或固件协议内部细节泄露给平台无关层。

代码 1-2 展示了启动上下文的核心字段组织。

#pseudo-sample("1-2", [启动上下文结构定义], kind: "代码")[
  ```c
  struct StartContext {
      struct StartBootInfo {
          enum Architecture architecture;    // 当前架构标识
          enum BootProtocol protocol;        // 启动协议类型
          size_t boot_cpu_id;                // 启动处理器硬件 ID
          const char* command_line;          // cmdline 字符串
      } boot;

      struct StartFirmwareTables {
          enum FirmwareSource selected;      // 固件选择标记
          struct Dtb* dtb;                   // DTB 视图
          struct AcpiTables {
              size_t rsdp_phys;              // RSDP 物理地址
              struct Mapping *mappings;      // 物理→虚拟映射表
              size_t mapping_count;          // 映射条目数
          } * acpi;                          // ACPI 视图
      } firmware;

      struct StartMemory {
          struct PhysRange kernel_image;     // 内核镜像物理范围
          union MemoryMap {
              struct EfiMemoryMap efi;       // EFI 内存映射快照
              struct MemoryRegion* regions;  // 规范化内存段数组
              void* none;                    // 无独立内存映射
          } boot_map;
      } memory;

      struct StartAddressOps {
          size_t (*phys_to_virt)(size_t);        // RAM 物理→虚拟
          size_t (*device_mmio_to_virt)(size_t); // MMIO 物理→虚拟
      } address;

      struct StartAllocatorOps {
          struct VirtRange (*kernel_heap_region)(void);
          void (*map_kernel_heap_range)(size_t, size_t, size_t);
          void (*unmap_kernel_heap_range)(size_t, size_t);
          void (*init_kernel_page_table)(void);
      } *allocator;  // 可选的分配器回调
  };
  ```
]

启动上下文的设计原则是只传递信息不传递机制。它不包含如何读取控制寄存器的函数指针，不包含如何操作页表的实现细节，也不包含特定架构的常量定义。相反，它只包含已经被架构加载器提取和稳定化的数据：内存段列表、固件表的内存快照、地址转换的闭包、以及固件选择的结果。

地址转换函数是启动上下文中最重要的抽象之一。不同架构有不同的早期地址映射机制，但无论底层机制如何，平台无关层只需要两个能力：把物理地址转换为可访问的虚拟地址，以及把设备物理地址转换为可访问的虚拟地址。架构加载器把这两个转换逻辑封装成函数指针，放入启动上下文，从而彻底隔离了地址映射的实现细节。平台无关层只需要调用这两个函数指针，而不需要知道底层是通过直接映射窗口、恒等映射还是临时页表实现的。

固件选择标记是另一个关键字段。架构加载器在复制固件表时，会根据固件的可用性和优先级选择唯一的固件来源，并在启动上下文中标记这个选择。平台无关层不会尝试重新选择或回退到其他固件源，而是严格按照这个标记进行解析。这种单向选择机制避免了固件解析的歧义，也使得启动日志能够清晰地反映系统到底信任了哪一种平台描述。

启动上下文的构造过程可以分为三个阶段：收集、稳定化和封装。收集阶段从固件接口读取原始数据，将固件传递的物理地址转换为当前可访问的虚拟地址，验证固件系统表的有效性。稳定化阶段把易失的固件数据复制到内核控制的内存区域，包括命令行、固件表、内存映射等。对于复杂的固件表（如 ACPI），需要递归发现并复制所有相关的表，同时维护一个物理地址到虚拟地址的映射表。封装阶段把稳定化的数据和架构能力组装成启动上下文，创建地址转换函数指针，填充固件选择标记，记录内核镜像的物理范围。

从这一刻开始，架构加载器的使命结束，启动上下文成为平台无关层唯一的信息来源。平台无关层不会回头调用架构加载器的任何函数,也不会尝试直接访问固件接口。所有需要的信息都已经被提炼、稳定化并封装在启动上下文中，等待被解释和转化为运行期子系统。

==== 启动上下文的构造细节

启动上下文的构造是一个精密的工程过程，它必须在有限的早期环境中完成复杂的数据收集、转换和封装工作。这个过程分为三个阶段：收集、稳定化和封装。

收集阶段的任务是从固件接口读取原始数据。不同架构、协议乃至平台传递启动参数的方式都是差异巨大的，架构加载器必须根据当前平台的启动协议，从正确的位置读取这些参数。

收集阶段的另一个关键任务是地址空间转换。固件传递的地址通常是物理地址，但架构加载器运行在虚拟地址空间中，必须将这些物理地址转换为可访问的虚拟地址。不同架构的早期地址映射机制差异巨大：LoongArch 通过直接映射窗口（DMW）将物理地址映射到固定的虚拟地址区域，x86 和 ARM 可能通过恒等映射或临时页表实现地址转换，RISC-V 可能在 S 模式下使用恒等映射。架构加载器必须根据当前架构的地址映射机制，实现正确的地址转换逻辑。

稳定化阶段的任务是把易失的固件数据复制到内核控制的内存区域。固件提供的数据可能位于固件服务内存中，一旦退出固件服务，这些内存可能被回收或覆写。因此，架构加载器必须在退出固件服务后，立即将这些数据复制到内核分配的缓冲区中。

对于简单的数据（如命令行字符串），稳定化过程相对直接：分配一个足够大的缓冲区，将字符串复制到缓冲区，记录缓冲区地址，最后把原本字符串所在的内存空间擦除，防止干扰内存分配器对用户态造成段错误。对于复杂的固件表（如 ACPI 表），稳定化过程要复杂得多。ACPI 表是一个树状结构，根表（RSDP）指向扩展系统描述表（XSDT），XSDT 包含多个表指针，每个表可能又包含子表。架构加载器必须递归遍历这个树状结构，发现所有相关的表，将它们复制到内核缓冲区，并维护一个物理地址到虚拟地址的映射表，使得平台无关层可以通过物理地址查找对应的虚拟地址。

DTB 的稳定化很简单，因为 DTB 是一个连续的二进制块，只需要复制整个块即可。但 DTB 的大小是可变的，架构加载器必须先读取 DTB 头部的大小字段，然后分配足够大的缓冲区。在实际测试中，大多数 DTB 的大小在几十 KiB 到几百 KiB 之间，因此分配 1 MiB 的缓冲区通常足够。对于 ACPI 表，情况更复杂，因为 ACPI 表的总大小取决于硬件配置的复杂度。在实际测试中，典型的 ACPI 表总大小在几百 KiB 到几 MiB 之间，因此分配 4 MiB 的缓冲区通常足够。如果缓冲区不足，架构加载器会报错并停止启动，而不是悄悄截断数据。

封装阶段的任务是把稳定化的数据和架构能力组装成启动上下文。这个阶段的关键是创建地址转换函数指针。地址转换函数是启动上下文中最重要的抽象之一，它隔离了不同架构的地址映射机制。

设备地址转换函数的实现与普通物理地址转换函数类似，但可能使用不同的映射窗口或页表。有些架构将设备地址映射到与普通内存不同的虚拟地址区域，以便区分内存访问和设备访问。有些架构使用相同的映射机制，此时设备地址转换函数和普通物理地址转换函数的实现相同。

分配器回调是启动上下文中唯一可选的字段。有些架构在启动阶段不需要动态分配内存，或者使用简单的线性分配器，此时分配器回调可以为空。有些架构需要在启动阶段建立完整的页表和堆管理，此时分配器回调必须提供四个函数：获取内核堆区域、映射内核堆范围、取消映射内核堆范围、初始化内核页表。这些函数的实现高度依赖具体架构，但它们的接口是统一的，平台无关层只需要调用这些函数指针，而不需要知道底层实现。

== 平台无关启动阶段

平台无关启动阶段的核心任务是解释启动上下文，激活运行期子系统，为用户态和调度器做准备。这个阶段完全不依赖具体的处理器架构和固件规范，所有需要的信息都已经被架构加载器提炼并封装在启动上下文中。平台无关层只需要按照统一的接口消费这些信息，将它们转化为运行期子系统的配置和状态。

=== 内核启动初始化

内核启动初始化是平台无关启动阶段的第一个环节，它接收启动上下文，解析固件表，注册电源控制，激活运行期分配器，注册设备和控制台，切换日志输出。代码 1-3 展示了内核启动初始化的核心流程。

#pseudo-sample("1-3", [内核启动初始化流程], kind: "代码")[
  ```c
  void kernel_start_init(const struct StartContext *ctx) {
      // 步骤 1：解析固件表
      parse_firmware_tables(ctx->firmware);

      // 步骤 2：注册电源控制
      register_power_control(ctx->firmware);

      // 步骤 3：激活运行期分配器
      init_physical_page_allocator(ctx->memory);
      bind_kernel_heap_mapping(ctx->allocator);
      init_virtual_memory_space(ctx->allocator);
      init_slab_allocator(ctx->allocator);
      activate_runtime_allocator(ctx->allocator);

      // 步骤 4：注册设备和控制台
      register_devices_from_firmware(ctx->firmware);
      register_console_from_firmware(ctx->firmware);
      switch_log_to_console();

      // 步骤 5：搜索并挂载根目录
      mount_devtmpfs();
      void* rootfs;
      if (has_initramfs()) {
        rootfs = get_initramfs();
      } else if (cmdline_exists("root")) {
        rootfs = find_name_for_blockdev(cmdline_find_for_key("root").value);
      } else if (devtmpfs_exists("vd0")) {
        rootfs = find_name_for_blockdev("/dev/vd0");
      } else { 
        kernel_panic();
      }
      mount_root(rootfs);

      // 步骤 6：跳转到主入口
      jump_to_main();
  }
  ```
]

内核启动初始化的编排原理基于子系统激活的依赖关系和资源可用性约束。

固件表解析必须最先完成，因为它是所有后续操作的信息来源。固件表中包含了处理器信息、中断控制器信息、设备信息、电源管理信息等，这些信息是注册电源控制、激活分配器、注册设备的前提条件。固件表解析的输出是一个统一的设备模型，无论底层是 ACPI 还是 DTB，上层看到的都是相同的设备抽象。

电源控制的注册必须在分配器激活之前完成，这是一个关键的安全约束。如果分配器激活失败（如内存不足、页表配置错误），系统可能需要立即重启或关机。此时电源控制必须已经可用，否则系统会陷入无法恢复的状态。这种"先建立退出机制，再执行危险操作"的模式是系统可靠性设计的基本原则。

分配器激活的内部顺序体现了分层依赖关系。物理页分配器是基础，它管理所有可用的物理内存；内核堆映射函数是物理页分配器的消费者，它将物理页映射到虚拟地址空间；虚拟内存空间管理器依赖物理页分配器和堆映射函数；slab 分配器依赖物理页分配器提供后备页；全局分配器的激活是最后一步，它标志着从启动分配器到运行期分配器的切换。这是一个典型的分层初始化模式，每一层都依赖下一层的服务。

设备和控制台的注册必须在分配器激活之后完成，因为设备对象和驱动实例需要动态分配内存。启动分配器不支持释放，如果在启动阶段分配大量设备对象，这些对象会永久占用内存。运行期分配器支持释放，因此可以安全地分配和释放设备对象。

日志输出的切换必须在控制台注册之后完成，因为运行期日志输出依赖控制台设备。切换时机的选择体现了"尽可能晚切换"的原则：在切换之前，早期日志输出一直可用，即使控制台注册失败，系统仍然可以输出诊断信息；切换之后，运行期日志输出提供更丰富的功能，但如果出现问题，早期日志输出已经不可用。这种权衡使得系统在启动阶段尽可能保持诊断能力。

虚拟文件系统的建立必须放在 `devtmpfs` 的挂载之后。在这里，我们寻找根目录所在位置的方法是这样的：如果存在 `initramfs`，我们认为根目录就是 `initramfs`；当其不存在时，我们看在 cmdline 中是否已经指定了 root 选项，如果已经指定，就采取已经指定的作为根目录；如果两者都不存在或者都无效，我们就强行认为根目录为 `/dev/vd0` 并强行挂载，如果这又是一个无效的根目录或者它也不存在，则直接 panic。

=== 主入口

主入口是本工程的 `main` 函数，它是平台无关启动阶段的最后一个环节，也是内核从启动阶段过渡到运行阶段的标志。当控制权到达主入口时，内核已经完成了所有基础设施的初始化：分配器可用、设备已注册、控制台已激活、日志系统已切换。此时的 `main` 函数拿到的不再是一个贫瘠的裸机环境，而是一个高度成熟的运行期子系统。主入口不再接收启动上下文，因为启动上下文是一次性交接对象，它只在架构加载器到内核启动初始化的控制权转移过程中存在。内核启动初始化已经将启动上下文中的信息转化为运行期子系统的配置和状态，主入口只需要使用这些已就绪的子系统。它的职责是启动用户态地址空间、调度器、系统调用表和第一个用户进程。

代码 1-4 展示了主入口的控制流。

#pseudo-sample("1-4", [主入口流程], kind: "代码")[
  ```c
  void main(void) {
      // 步骤 1：初始化调度子系统
      init_task = sched_boot_init();

      // 步骤 2：启动用户态 init 进程
      start_init_process(init_task);
  }
  ```
]

调度子系统的初始化通过 `sched_boot_init()` 完成。它依次执行以下操作：注入架构相关的上下文切换和时间源钩子、注册任务扩展拷贝钩子（用于 fork/clone 时处理 VFS 上下文和文件描述符表的共享或深拷）、注入用户进程镜像操作表、注册虚拟内存切换钩子、创建 init 任务并分配 PID 1、为 init 任务挂载 VFS 根上下文和文件描述符表、为 CPU 0 启动 idle 内核线程。

用户态 init 进程的启动通过 `start_init_process()` 完成。它按优先级尝试 `/init`、`/sbin/init`、`/bin/init` 三个路径，加载第一个存在的 ELF 镜像，配置用户态地址空间、入口点和初始栈，然后通过陷阱帧恢复跳转到用户态。从此，内核把控制权交给 init 进程，操作系统进入了真正意义上的稳定化运行阶段。

== 工程设计总结

本章描述的启动控制流体现了"逐层降低不确定性"的核心设计理念。固件将控制权交给内核时，处理器可能处于各种不同的状态，内存布局可能千差万别，硬件描述可能采用不同的格式。架构相关启动阶段的职责是将这些不确定性逐步消除：统一入口建立最小的硬件访问环境，高级语言环境准备建立函数调用所需的栈和调用约定，架构加载器收集并稳定化固件信息，启动上下文将这些信息封装成统一的接口。平台无关启动阶段接收这个统一的接口，不再需要关心底层的硬件差异，只需要按照统一的逻辑解析固件表、激活子系统、启动用户态。

单向信息流是这个设计的另一个关键特征。信息从底层流向高层，从架构相关层流向平台无关层，从启动阶段流向运行阶段。这种单向流动避免了循环依赖，使得每一层都有清晰的职责边界。架构加载器不需要知道内核如何使用启动上下文，内核启动初始化不需要知道启动上下文是如何构造的，主入口不需要知道启动上下文的存在。每一层只需要完成自己的任务，将结果传递给下一层，然后退出历史舞台。

这种设计的工程价值在于可维护性和可扩展性。当需要支持新的处理器架构时，只需要实现新的架构加载器，提供符合启动上下文接口的实现，平台无关层的代码无需修改。当需要支持新的固件协议时，只需要在架构加载器中添加新的固件解析逻辑,平台无关层的代码无需修改。当需要改进运行期子系统时，只需要修改平台无关层的代码，架构相关层的代码无需修改。这种分离使得项目可以在多个维度上独立演化，而不会因为一个维度的变化而影响其他维度。

最后，这种设计体现了"以道驭器，以经统权"的哲学思想。平台无关的高层逻辑是道，它不依赖于特定的硬件实现，提炼出操作系统最本质的法则与策略。平台相关的底层处理是器，它将道落实到具体的硬件平台上，处理寄存器、页表、异常入口这些物理世界的细节。启动上下文是道与器的交接点，它将器的复杂性封缄其中，只向道暴露必要的能力和数据。这种分离使得道可以保持稳定和通用，器可以灵活适应不同的硬件平台，两者相辅相成，共同构成了一个可维护、可扩展的操作系统内核。
