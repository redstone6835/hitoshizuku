#import "../config.typ": preface-title, project-name
#import "../styles/navigation.typ": manual-front-section

#manual-front-section(preface-title)[
MyGO!!!!! OS 是 2026 年全国大学生计算机系统能力大赛操作系统设计赛（OS 内核实现赛道）的参赛作品，采用 Rust 语言编写。本项目的核心目标是构建一个分层清晰、职责明确的内核工程结构，使得不同架构、不同固件协议、不同启动方式的适配工作能够在统一的框架下进行，而无需修改上层策略代码。

“以不变应万变”，这是我们最重要的设计哲学。处理器指令集层出不穷，固件协议更迭频繁，硬件平台的差异更是不可胜数。面对这些变量，我们不寻求在每一处细节上都做适配，而是追问一个问题：在所有平台的所有启动路径中，究竟哪些事实是恒定不变的？无论从何种固件接口获取硬件信息，无论通过何种协议进入内核，最终的逻辑终点总是相同的：系统需要知道自己占用了哪些物理内存、可用哪些设备、控制台如何输出、输出到哪里、处理器拓扑如何，等等。这些需求独立于任何具体的指令集或固件格式而存在。

于是我们把这些不变的需求提炼为稳定的抽象层，同时将变化的部分逐层封存于底层模块之中。每一层都只面对自己应当面对的复杂性，不越界、不回流。变化的只是外部的入口和底层的实现，不变的是内核的策略框架和模块之间的契约。当新的架构或固件规范出现时，我们只需替换底层的适配层，而无需改动上层的任何一行策略代码。以契约取代依赖，以单向信息流消除耦合，将变化隔离、将不变凝聚，让每一层只面对它应当面对的复杂性。这不是技术上的妥协，而是一种主动的秩序建构——在混乱的硬件世界中，划定一片理性可以掌控的疆域。

本手册的工程目的是为项目提供一份系统化的技术说明，帮助读者快速建立对内核整体图景的认知。同时，手册也为项目成员提供了统一的术语和设计约定。在多人协作的开发过程中，不同模块的实现者往往只熟悉自己负责的那一部分。手册明确了分层边界、模块契约和控制权的单向流动规则，使得每位成员都能够在不阅读全部源码的情况下，准确理解自己所在模块的前提条件、输出承诺和与其他模块的交互方式。这显著降低了协作摩擦，也使得代码评审和新功能开发可以在明确的架构原则下进行，而不依赖于个人经验或口头约定。

由于手册编写工作展开得较为匆忙，部分章节的叙述密度可能不均匀，个别图表和示例仍有待进一步打磨。我们会随着内核的持续开发逐步修订和补充手册内容，使其更准确地反映代码的实际结构和设计意图。读者在阅读过程中如发现表述不清或与实际实现不一致之处，欢迎提出反馈，帮助我们改进。
]
