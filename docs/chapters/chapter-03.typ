#import "../config.typ": project-name
#import "../styles/diagram.typ": flow-arrow, flow-node, layer-card
#import "../styles/figure.typ": continued-table, figure-caption, pseudo-sample
#import "../styles/tokens.typ": handoff-fill, soft-fill, stable-fill, warm-fill

= 第三章 设备模型与驱动框架

在第二章中，我们讨论了内存管理子系统如何通过分层递进的方式，从最原始的线性分配器逐步构建出完整的内存管理能力。这种"渐进式能力构建"的思路并非内存管理所独有，它同样适用于设备管理。内核在启动早期面对的是一片沉默的硬件，处理器发现了内存，但所有外设，包括串口、块设备和网络控制器，都还没有被纳入管理。内核需要一套机制来发现这些设备、为它们加载驱动、将驱动暴露的 I/O 能力封装为统一的接口，最终使上层子系统（如文件系统和网络协议栈）可以通过一致的抽象访问所有硬件。这就是设备模型与驱动框架要解决的问题。

然而，设备模型面临着一个与内存管理截然不同的挑战，那就是硬件的异构性。内存管理面对的资源本质上是同构的，每个物理页帧大小相同、行为一致，只需解决分配和回收的问题。但设备天生就是异构的：串口以字节流的方式传输数据，块设备以固定大小的扇区进行随机读写，网络控制器以数据包为单位收发帧。如果试图用单一抽象覆盖所有设备，结果要么是抽象过于粗糙，搞得所有设备都被降格为字节流，块设备的随机访问能力被浪费；要么是抽象过于复杂，迫使接口需要同时支持流式访问、块访问和数据包访问，调用方不得不理解所有模式。面对这个困境，我们选择了"双轨抽象"的方案：字符设备覆盖流式设备，块设备覆盖随机访问设备，两者各自提供最适合自己设备类型的接口，而非强行统一到一个不自然的抽象之下。

这种双轨抽象的选择并非没有先例。Unix 自诞生之初就将设备分为"字符设备"和"块设备"两类，通过主次设备号区分。但 Unix 的传统做法存在一个根本性问题：字符设备和块设备共享同一个 `read`/`write` 接口，区别仅在于内核是否使用缓冲区缓存。这实际上是把"设备的物理特性"和"内核是否缓存"两个正交的问题耦合在了一起。我们的设计将这两个问题解耦，让字符设备和块设备拥有完全不同的接口签名，接口的差异反映的是设备行为模式的本质不同，而非缓存策略的不同。缓存策略是上层文件系统的决策，不应泄漏到设备接口中。

在深入双轨抽象的细节之前，有必要先理解设备模型的整体分层。设备模型从底至上分为三层：功能层定义了字符设备和块设备两种 I/O 抽象，并通过全局设备枚举统一管理所有已注册的设备对象；PnP 层为可枚举总线上的动态设备提供生命周期管理，包括硬件身份、驱动匹配、状态机和热插拔处理；devtmpfs 层将功能层的设备对象映射为文件系统中的设备节点，使用户程序可以通过 POSIX 文件接口访问设备。

#figure(caption: figure-caption("图", "3-1", [设备模型分层架构]))[
  #layer-card("devtmpfs 层", "将设备对象映射为 /dev 下的设备节点", fill: stable-fill)
  #flow-arrow(label: "绑定设备节点")
  #layer-card("功能层", "CharDevice / BlockDevice 全局设备枚举（核心注册中心）", fill: soft-fill)
  #flow-arrow(label: "直接注册          PnP 功能注册")
  #layer-card("静态设备", "启动时直接构造驱动实例", fill: warm-fill)
  #layer-card("PnP 层 + 总线扫描", "PCI/USB 扫描 → 驱动匹配 → probe → 注册", fill: handoff-fill)
]

这三层之间的关系并非简单的线性堆叠。功能层是设备模型的核心注册中心，所有设备最终都以 `CharDevice` 或 `BlockDevice` 的形式注册到全局设备枚举中，但到达功能层的路径有两条。第一条是直接注册路径：启动阶段的静态设备（如串口）在固件描述解析期间被直接识别，启动代码构造驱动实例并将其包装为功能设备对象，直接推入全局设备枚举，整个过程不经过 PnP 层。第二条是 PnP 中介路径：可枚举总线（PCI、USB）扫描硬件后创建 PnP 设备对象，PnP 层负责匹配驱动并调用 `probe`，驱动在 `probe` 中初始化硬件并通过 PnP 设备的功能注册方法将设备推入全局设备枚举。devtmpfs 层位于功能层之上，无论设备通过哪条路径注册，最终都会被绑定到 `/dev` 目录中的设备节点。之所以采用这种双路径设计而非强制所有设备经过 PnP 层，是因为启动阶段的核心设备必须在 PnP 框架就绪之前完成初始化——内核需要在最早期就拥有串口输出能力以便调试，而此时 PnP 框架的驱动注册表和 devtmpfs 的回调机制都尚未建立。

== 字符设备抽象

字符设备是设备模型中覆盖流式设备的抽象。所谓"流式"，是指数据的传输没有固定的边界和寻址能力，调用方只能按顺序读写数据，不能指定读写的起始位置。串口、终端和随机数发生器都是典型的流式设备。对于这样的设备，我们不难发现，无论是什么样的流式设备，都必须要具备两大核心功能，也就是输入与输出。至于如何做到输入和输出，这是每个设备所特有的，在抽象层我们不需要管。所以作为字符设备驱动的抽象，我们只需要提供相关输入输出的方法就可以了。

=== 阻塞语义的显式分离

字符设备抽象的核心接口是 `CharDriver`，它定义了四个核心方法：非阻塞写、非阻塞读、阻塞刷新和阻塞全量写。这种设计的核心思想是将阻塞语义的控制权交给调用方，而非由驱动接口强制规定。非阻塞写将缓冲区中尽可能多的字节送入设备，返回实际被设备接受的字节数，当设备无法接受数据时返回零。非阻塞读从设备中读取尽可能多的字节到缓冲区，返回实际可用的字节数，当设备没有数据时返回零。阻塞刷新等待设备内部缓冲区中的数据全部排空。阻塞全量写反复调用非阻塞写，直到缓冲区中的所有字节都被设备接受。

之所以要把阻塞和非阻塞拆分为不同的方法，而不是像传统 Unix 那样通过 `O_NONBLOCK` 标志来切换，根源在于内核内部的不同调用方对阻塞语义的需求各不相同。比如说，控制台输出路径需要非阻塞写，这是因为内核不能因为串口 FIFO 满而无限等待，否则系统会失去响应；而日志刷盘路径就需要阻塞刷新，因为日志系统必须确保所有数据已经物理发出，才能继续执行。如果只有一种带 `O_NONBLOCK` 标志的接口，内核内部的调用方就必须自己实现"循环直到完成"的逻辑，这不仅需要重复写大量逻辑，而且很容易出错，因为对于不同的设备，一旦循环中缺少超时逃生，很可能会导致某些硬件故障时系统死锁。将阻塞和非阻塞拆分为不同的方法，使得每种调用方都可以直接使用最适合自己需求的接口，无需自己组装阻塞逻辑。

以下代码展示了字符设备驱动接口的定义，所有方法均通过 `&self` 调用，实现方在内部自行处理并发保护：

#pseudo-sample("3-1", [字符设备驱动接口定义], kind: "代码")[
  ```rust
  trait CharDriver: Send + Sync {
      fn write(&self, buf: &[u8]) -> Result<usize, CharIoError>;
      fn read(&self, buf: &mut [u8]) -> Result<usize, CharIoError>;
      fn flush(&self) -> Result<(), CharIoError>;
      fn write_all(&self, buf: &[u8]) -> Result<(), CharIoError>;
      fn poll_write(&self);
      fn as_any(&self) -> &dyn Any;
  }
  ```
]

`write_all` 的默认实现通过循环调用非阻塞写并加入超时逃生来保证整个缓冲区被写入设备，同时防止硬件故障导致内核死锁。

#pseudo-sample("3-2", [`write_all` 的默认实现逻辑], kind: "代码")[
  ```c
  void write_all(CharDriver* dev, const uint8_t* buf, size_t len) {
      const uint8_t* remaining = buf;
      size_t retries = 0;
      while (len > 0) {
          size_t n = dev->write(remaining, len);
          if (n > 0) {
              remaining += n;
              len -= n;
              retries = 0;
          } else {
              retries++;
              if (retries > MAX_WRITE_RETRIES)
                  return CharIoError_Timeout;
              dev->poll_write();
              spin_loop_hint();
          }
      }
  }
  ```
]

`write_all` 的默认实现揭示了一个重要的设计原则：驱动接口的默认方法不仅是便利函数，更是正确性的保障。如果没有默认实现，每个驱动都需要自己编写"循环写入加超时逃生"的逻辑，而这段逻辑恰恰是最容易出错的地方。忘记超时逃生会导致死锁，忘记调用 `poll_write` 会导致带软件发送缓冲的驱动无法排空。默认实现将这些容易出错的逻辑集中到一处，驱动只需实现非阻塞写和阻塞刷新，就可以自动获得正确的阻塞全量写行为。

=== 错误类型的精简设计

字符设备的错误类型 `CharIoError` 只包含三种变体：`HardwareError` 表示硬件级错误（帧错误、奇偶校验错误、溢出等）；`Unavailable` 表示设备不可用或已断开；`Timeout` 表示自旋等待超时。这种精简的错误分类是有意为之的。字符设备的调用方通常无法从错误中恢复，它们需要的只是一个"操作失败"的信号，而非详细的错误诊断。例如，控制台输出路径在遇到写入错误时只能丢弃数据并继续运行，不会因为奇偶校验错误而采取不同的恢复策略。因此，过于详细的错误分类只会增加驱动实现的负担，而不会为调用方带来实际价值。

=== 字符设备对象与状态管理

字符设备对象（`CharDevice`）是对驱动的轻量级包装，它记录了设备的种类（`CharDeviceKind`）、固件名称（`fw_name`）和状态（`Active` 或 `Gone`）。其中，固件名称是设备在注册时分配的唯一标识，由创建设备的代码生成（对于静态设备，通常是 MMIO 地址；对于 PnP 设备，由总线扫描代码根据硬件拓扑生成）；状态字段用于支持热插拔，当设备被移除时，状态被标记为 `Gone`，后续的 I/O 操作将返回 `Unavailable` 错误。

字符设备对象的核心结构如下代码所示，它通过 `Arc` 实现共享所有权，通过原子状态字段支持热插拔标记：

#pseudo-sample("3-3", [字符设备对象的核心结构], kind: "代码")[
  ```rust
  struct CharDevice {
      inner: Arc<CharDeviceInner>,
  }
  struct CharDeviceInner {
      kind: CharDeviceKind,
      fw_name: &'static str,
      driver: &'static dyn CharDriver,
      state: AtomicU8,    // Active=0, Gone=1
  }
  ```
]

`CharDevice` 使用 `Arc` 共享所有权，这意味着多个子系统可以同时持有对同一个字符设备的引用。例如，devtmpfs 的设备节点、全局设备枚举和正在进行的 I/O 操作都可能同时持有同一个字符设备的 `Arc`。当设备被移除时，`mark_gone` 将状态原子地设置为 `Gone`，但 `Arc` 的引用计数不会立即归零，只有当所有持有者都释放了引用，`CharDeviceInner` 才会被回收。这种"先标记后回收"的策略与 PnP 框架的分阶段移除流程一脉相承，它保证了在设备消失的整个过程中，系统的每个部分都能安全地感知到设备的状态变化。

`CharDevice` 的所有 I/O 方法在委托给驱动之前都会先检查设备状态。如果设备已经标记为 `Gone`，方法立即返回 `Unavailable` 错误，不会调用驱动的任何方法。这种前置检查保证了驱动不会被已移除的设备调用，消除了使用已释放资源的风险。同时，`downcast_driver` 方法在设备 `Gone` 时返回 `None`，防止调用方通过向下转型访问已卸载的驱动。

== 块设备抽象

块设备是设备模型中的另一类核心抽象。与字符设备的流式传输不同，块设备以固定大小的块（通常为 512 字节或 4096 字节）为单位进行读写。典型的块设备包括硬盘、固态硬盘和虚拟块设备。块设备抽象之所以需要与字符设备分离，是因为块设备具备两个流式设备不可能具备的核心特征。其一是可寻址性，调用方可以指定读写的起始位置和长度，而不需要按顺序访问数据；其二是异步性，块设备的 I/O 操作天然是异步的，调用方提交请求后可以继续执行其他工作，当 I/O 完成时通过回调获得通知。

块设备的异步性并非可有可无的优化，而是由硬件的物理特性决定的。磁盘的寻道时间是毫秒级的，固态硬盘的读取延迟是微秒级的，而处理器的指令周期是纳秒级的。如果每次 I/O 操作都阻塞等待完成，处理器将在数百万个指令周期内无所事事。异步提交允许调用方同时发起多个 I/O 请求，充分利用硬件的队列能力和并行处理能力。例如，文件系统可以同时提交多个脏页的写回请求，块设备驱动可以将这些请求排序和合并以减少磁盘寻道距离。

=== 异步提交与完成回调

块设备驱动接口的核心是 `submit` 方法，它接收一个请求对象并异步提交给设备。请求对象包含了操作类型（读或写）、起始扇区号、扇区数量、数据缓冲区和完成回调。驱动在收到请求后，将其放入内部的请求队列，然后立即返回。当硬件完成 I/O 操作后，驱动调用完成回调通知调用方。以下代码展示了块设备请求与提交接口的定义：

#pseudo-sample("3-4", [块设备请求与提交接口], kind: "代码")[
  ```rust
  struct BlockRequest {
      kind: BlockRequestKind,
      start_sector: u64,
      sector_count: u32,
      data: BlockRequestData,
      callback: Box<dyn FnOnce(BlockRequestResult) + Send + Sync>,
  }

  trait BlockDriver: Send + Sync {
      fn geometry(&self) -> BlockGeometry;
      fn limits(&self) -> &BlockLimits;
      fn submit(&self, req: BlockRequest) -> Result<(), BlockSubmitError>;
      fn as_any(&self) -> &dyn Any;
  }
  ```
]

完成回调使用 `FnOnce` 而不是通道（channel）或轮询（poll），是因为 `FnOnce` 回调是最轻量的异步通知机制。它不需要分配额外的数据结构，不需要维护通道的缓冲区，不需要轮询的就绪检查。当 I/O 完成时，驱动直接在完成中断的上下文中调用回调函数，回调函数可以立即处理结果，无需额外的上下文切换。相比之下，通道和轮询机制都需要额外的中间数据结构来传递结果，增加了内存分配开销和延迟。当然，`FnOnce` 回调的局限在于不支持多消费者的情形，换句话说，在一个请求的情况下只能有一个回调，但对于块设备而言，每个请求只有一个提交者，单消费者模型已经足够。

=== 几何信息与能力限制

块设备的几何信息（`BlockGeometry`）描述了设备的物理参数，包括总容量、扇区大小、是否只读等。块设备的限制（`BlockLimits`）描述了驱动对请求的约束，包括最大扇区数、最大段数、段对齐要求等。这些信息在文件系统挂载时被查询，文件系统根据这些参数来决定自己的块大小、预读策略和写回策略。例如，如果块设备报告最大扇区数为 256，文件系统就不会提交超过 128KB 的单个请求；如果块设备报告段对齐要求为 4096 字节，文件系统就会确保数据缓冲区的起始地址是 4096 字节对齐的。

将几何信息和限制分开，而非合并为一个结构体，是因为几何信息是设备的固有属性，不会因驱动的实现方式而改变；而限制是驱动的能力约束，同一个设备的不同驱动可能有不同的限制。例如，一个简单的轮询驱动可能只支持单段请求，而一个使用 DMA 的驱动可能支持多段请求。将两者分开，使得文件系统可以独立查询设备的物理参数和驱动的能力约束，而不需要理解两者之间的关联。

=== 块设备对象与生命周期

块设备对象（`BlockDevice`）与字符设备对象类似，也是对驱动的轻量级包装，但它使用 `Arc<BlockDevice>` 而非 `CharDevice` 的 `Arc<CharDeviceInner>` 模式。这种差异源于两者在生命周期管理上的不同需求。字符设备的驱动通常是静态分配的（如 Uart16550 的静态实例），驱动引用使用 `&'static dyn CharDriver`，不需要引用计数；块设备的驱动可能由模块加载器动态分配，驱动引用使用 `Arc<dyn BlockDriver>`，需要引用计数来管理驱动的生命周期。这种差异也反映在功能注册的 API 上：字符设备注册时传入 `CharDevice`（值类型，内部包含 `Arc`），块设备注册时传入 `Arc<BlockDevice>`（显式的共享句柄）。

块设备对象还维护了一个 I/O 计数器，用于跟踪当前正在进行的 I/O 操作数量。在热插拔的排空阶段，PnP 框架等待 I/O 计数器归零，确保所有正在进行的 I/O 都已完成，然后才调用驱动的 `remove` 方法。这种基于计数器的排空机制比基于锁的排空机制更加高效，它不需要阻塞新的 I/O 提交（新提交会被 `Gone` 状态拒绝），只需要等待已有的 I/O 自然完成。

=== 三层错误分类

块设备的错误分类比字符设备更加精细，因为块设备的调用方（文件系统）可以从某些错误中恢复。`BlockIoError` 包含 `MediaError`（介质错误）、`Unavailable`（设备不可用）、`Timeout`（超时）、`ReadOnly`（写只读设备）和 `Unsupported`（不支持的操作）。`BlockRequestError` 包含 `EmptyRange`（空范围）、`OutOfBounds`（越界）、`TooLarge`（请求过大）、`BufferSizeMismatch`（缓冲区大小不匹配）和 `Misaligned`（未对齐）。`BlockSubmitError` 包含 `Unsupported`、`ReadOnly`、`QueueFull`（队列满）、`DeviceGone`（设备已消失）、`OutOfMemory` 和 `InvalidRequest`。这种三层错误分类使得调用方可以精确判断错误的来源和可恢复性，例如硬件错误通常不可恢复，文件系统应向上层报告 I/O 错误；请求错误是调用方的编程错误，不应在正常运行时出现；提交错误中的 `QueueFull` 是可恢复的，调用方可以等待后重试。

== 驱动控制接口

字符设备和块设备的读写接口覆盖了设备最常见的操作，但有些设备支持超出读写范畴的特定命令，例如串口的波特率设置、块设备的缓存刷新命令。传统 Unix 系统通过 `ioctl` 系统调用来传递这些设备特定的命令，`ioctl` 接受一个整数命令码和一个可变参数，驱动根据命令码解释参数的语义。这种设计有一个根本性问题：编译器无法在编译期验证命令码与参数类型之间的对应关系。如果调用方传入了错误的命令码或错误的参数类型，驱动只能在运行时通过 `EINVAL` 等错误码来检测，而有些错误甚至不会触发错误返回，而是导致静默的数据损坏。因此，利用 Rust 的语言特性引入一个类型安全的控制命令对于数据的安全性是很有帮助的。

=== 类型安全的控制命令

我们的驱动控制接口（`DriverControl`）采用了完全不同的设计。每种驱动自行定义请求、响应和错误三个关联类型，控制方法接受类型安全的请求对象，返回类型安全的响应或错误。编译器在调用端即可验证请求与响应类型匹配，不需要运行时检查。以下代码展示了驱动控制接口的定义：

#pseudo-sample("3-5", [驱动控制接口定义], kind: "代码")[
  ```rust
  trait DriverControl {
      type Request;
      type Response;
      type Error;
      fn control(&self, req: Self::Request) -> Result<Self::Response, Self::Error>;
  }
  ```
]

以串口驱动为例，`Uart16550` 结构体实现了 `DriverControl` trait，关联类型分别为 `UartRequest`、`UartResponse` 和 `UartError`。`UartRequest` 包含 `SetBaudRate` 和 `GetLineStatus` 两个变体，`UartResponse` 包含 `Done` 和 `LineStatus` 两个变体，`UartError` 包含 `InvalidBaudRate` 等变体。当调用方想要修改波特率时，它构造 `UartRequest::SetBaudRate { clock_hz, baud }` 并调用 `control` 方法，编译器在编译期就能验证请求参数的正确性。如果调用方误传了 `GetLineStatus` 请求，但期望得到 `Done` 响应，编译器会直接报类型不匹配的错误，而不是在运行时返回 `EINVAL`，从根源上杜绝了这种错误。

这种设计相较于传统 `ioctl` 接口，最大的优势在于将运行时错误前移到编译期。在内核开发中，运行时错误往往意味着系统崩溃或安全漏洞，而编译期错误只需修改代码重新编译。类型安全的控制接口通过关联类型将请求、响应和错误三者绑定在一起，使编译器可以在编译期验证所有控制命令的参数正确性。这种设计的代价是增加了驱动实现的复杂度，但收益极其显著。

=== 向下转型与类型恢复

驱动控制接口的类型安全在通用路径上面临一个挑战。当调用方通过 `dyn CharDriver` 访问设备时，`DriverControl` 的关联类型在类型擦除后不可见，调用方无法构造类型安全的请求对象。为了解决这个问题，`CharDriver` 提供了 `as_any` 方法返回 `&dyn Any` 引用，调用方可以通过向下转型恢复具体的驱动类型。`CharDevice` 在此基础上提供了 `downcast_driver<T>` 便利方法，封装了 `as_any` 和 `downcast_ref` 两个步骤。

这种设计在通用性和类型安全之间找到了一个平衡点。通用路径保证了所有字符设备都可以用同一种方式访问（读写和刷新），而特定路径保证了设备控制命令的类型安全性。调用方在需要设备特定功能时，通过向下转型恢复具体驱动类型，然后调用类型安全的控制方法。这种窄门设计确保了类型安全不会被绕过，调用方必须显式地选择使用设备特定功能，而不是隐式地依赖类型擦除后的行为。

== 即插即用（PnP）设备

PnP 设备框架是设备模型中管理设备生命周期和驱动绑定的核心子系统。其设计目标是统一不同总线上的设备管理逻辑，使 PCI 设备、USB 设备和平台设备都可以用同一个模型来管理，享受统一的状态机、驱动匹配和热插拔处理机制。

=== 硬件身份与总线拓扑

PnP 设备的身份由 `PnpId` 标识，它是一个枚举类型，包含 PCI、USB 和 Platform 三种变体。PCI 变体记录段号、总线号、设备号和功能号；USB 变体记录总线 ID、设备地址和接口号；Platform 变体记录设备名称和索引。`PnpId` 实现了 `Hash` 和 `Eq`，可以作为哈希表的键使用，PnP 设备列表通过 `PnpId` 查找设备。

PnP 设备之间可以形成父子拓扑关系。比如说一个 USB 控制器是 PCI 总线上的设备，USB 控制器下的集线器是控制器的子设备，集线器下的功能设备是集线器的子设备。PnP 设备通过 `attach_child` 方法建立父子关系，父设备持有子设备的 `Arc` 强引用，子设备持有父设备的 `Weak` 弱引用。弱引用的使用避免了循环引用导致的内存泄漏，当父设备被移除时，子设备可以通过 `upgrade` 检测到父设备已经消失。

=== 状态机与驱动绑定

PnP 设备的生命周期由一个严格的状态机管理。状态机的状态包括 `Discovered`（已发现但未绑定驱动）、`Probing`（正在尝试绑定驱动）、`Bound`（已绑定驱动，正常工作）、`Removing`（正在移除）和 `Gone`（已移除）。状态转换规则通过 `can_transition_to` 方法静态定义，只允许合法的转换，例如从 `Discovered` 到 `Probing`、从 `Probing` 到 `Bound`、从 `Bound` 到 `Removing`、从 `Removing` 到 `Gone`。任何非法的状态转换都会返回 `InvalidTransition` 错误。

#figure(caption: figure-caption("图", "3-2", [PnP 设备状态机]))[
  #flow-node("Discovered", fill: warm-fill)
  #flow-arrow(label: "匹配驱动")
  #flow-node("Probing", fill: soft-fill)
  #flow-arrow(label: "probe 成功")
  #flow-node("Bound", fill: stable-fill)
  #flow-arrow(label: "移除请求")
  #flow-node("Removing", fill: handoff-fill)
  #flow-arrow(label: "清理完成")
  #flow-node("Gone", fill: warm-fill)
]

驱动匹配算法采用"先注册先匹配"的策略。PnP 框架维护一个全局的驱动注册表（`PnpDriverRegistry`），当新设备注册并调用 `probe_device` 时，框架遍历注册表中对应总线类型的驱动列表，对每个驱动调用 `matches` 方法检查其是否支持该设备的硬件身份。第一个匹配成功的驱动被选中，框架调用其 `probe` 方法。如果没有驱动匹配，设备状态回退到 `Discovered`，框架返回 `NoDriver` 错误。这意味着当前实现依赖于"先注册驱动，再扫描设备"的调用顺序——总线扫描代码在启动时先将所有驱动注册到注册表，然后再枚举硬件并为每个发现的设备调用 `probe_device`。这种顺序约束在启动阶段是自然满足的，因为驱动注册是编译时确定的静态操作，而设备扫描是运行时的动态操作。

驱动注册表按总线类型分组，包含 `pci_drivers`、`usb_drivers`、`platform_drivers` 和 `generic_drivers` 四个子列表。匹配时先在对应总线类型的子列表中查找，如果没有找到匹配的驱动，再回退到 `generic_drivers` 列表。这种分组策略减少了匹配时的遍历范围，提高了匹配效率。例如，一个 PCI 设备只需要遍历 PCI 驱动列表和通用驱动列表，不需要遍历 USB 驱动列表。

PnP 驱动接口（`PnpDriver`）定义了五个核心方法。`name` 返回驱动的名称。`bus_type` 返回驱动所属的总线类型。`matches` 检查驱动是否支持给定的设备身份。`probe` 在驱动与设备匹配成功后被调用，负责初始化硬件并注册功能设备。`remove` 在设备被移除时调用，负责关闭硬件并清理驱动私有状态。`probe` 方法接收 `&Arc<PnpDevice>` 作为参数，驱动可以通过这个参数访问设备的硬件身份、拓扑信息和父总线提供的资源。

`probe` 方法的实现通常遵循一个固定的模式。首先，驱动从 PnP 设备的硬件身份中提取自己需要的资源，例如 MMIO 基地址、中断号或 DMA 通道。然后，驱动根据这些资源创建具体的驱动实例，初始化硬件。最后，驱动将驱动实例包装为功能设备（字符设备或块设备），调用 PnP 设备的 `register_char_function` 或 `register_block_function` 方法注册到全局设备枚举和 devtmpfs。如果任何一步失败，驱动回滚已完成的步骤并返回错误，PnP 框架将设备状态回退到 `Discovered` 并返回 `NoDriver` 或 probe 错误。由于当前实现不支持自动重试其他驱动，probe 失败的设备将保持在 `Discovered` 状态，直到外部代码再次显式调用 `probe_device`。

=== 热插拔与分阶段移除

热插拔是 PnP 框架最复杂的功能之一。对于一个 PnP 设备，它可以在任意时刻被创建或移除，而内核必须保证移除过程中不会出现访问已释放资源的情况。PnP 框架通过严格的分阶段移除流程来保证这一点。

移除流程分为八个阶段。第一阶段，标记 `Removing` 状态，阻止新的驱动绑定。第二阶段，递归移除子设备，采用深度优先策略，先移除叶子节点。第三阶段，标记所有功能设备为 `Gone`，阻止新的 I/O。第四阶段，排空已有的 I/O，等待所有正在进行的块设备请求完成。第五阶段，调用驱动的 `remove` 方法，安全关闭硬件。第六阶段，从 devtmpfs 和全局设备枚举中解绑功能设备。第七阶段，标记 `Gone` 状态。第八阶段，从全局 PnP 设备列表中移除。

这个流程的核心原则是"先阻止、再排空、后释放"，换句话说，我们会先阻止新的访问和 I/O，再排空已有的 I/O，最后释放资源。这意味着，在资源释放之前，所有可能访问这些资源的路径都已被切断，因此不会出现访问已释放内存或调用已卸载驱动方法的情况，可以避免产生一些奇怪的竞态条件。这种设计虽然增加了移除流程的延迟，但保证了移除过程的安全性，而安全性在内核开发中永远优先于性能。

移除流程还使用了一个 `removal_lock` 原子布尔值来防止并发移除。当多个调用方同时请求移除同一个设备时，只有第一个成功执行 `compare_exchange` 的调用方会进入移除流程，后续的调用方会直接返回。这种轻量级的锁机制避免了使用互斥锁的开销，同时保证了移除操作的原子性。

=== 功能注册与事务式策略

功能注册是 PnP 设备将 I/O 能力暴露给上层的过程。PnP 设备本身不提供 I/O 接口，它只是一个硬件管理实体。I/O 能力通过功能设备（字符设备或块设备）暴露。驱动在 `probe` 成功后，调用 PnP 设备的 `register_char_function` 或 `register_block_function` 方法，将功能设备注册到全局设备枚举和 devtmpfs。这两个方法采用事务式注册策略——"任一步骤失败则回滚已完成的步骤"。以块设备功能注册为例：首先将块设备推入全局设备枚举的块设备子列表，然后尝试在 devtmpfs 中绑定设备节点；如果 devtmpfs 绑定失败，就从全局设备枚举中移除刚添加的块设备，回滚到注册前的状态。这种事务式注册保证了全局设备枚举和 devtmpfs 之间的一致性，不会出现"设备在枚举中但不在 devtmpfs 中"或反之的不一致状态。

PnP 框架与 devtmpfs 之间通过回调机制通信。PnP 框架定义了 `PnpDevtmpfsCallbacks` 结构体，包含 `bind_block`、`bind_char` 和 `unbind` 三个函数指针。devtmpfs 在挂载完成后注册这些回调，PnP 框架在注册功能设备时调用回调来创建或删除设备节点。使用回调而非直接调用 devtmpfs 的方法，是因为 PnP 框架位于设备模型层，而 devtmpfs 位于文件系统层。如果 PnP 框架直接调用 devtmpfs 的方法，就产生了从设备模型层到文件系统层的反向依赖，违反了单向依赖原则。回调机制将依赖关系反转——devtmpfs 依赖 PnP 框架提供的回调注册接口，PnP 框架不依赖 devtmpfs 的任何代码。这种设计使得 PnP 框架可以在 devtmpfs 未就绪时正常工作：此时回调未被注册，PnP 框架在注册功能设备时跳过 devtmpfs 的绑定步骤，设备仍然可以通过全局设备枚举访问，只是不会出现在 `/dev` 目录中。

== 设备初始化流程

设备模型的初始化遵循一个确定的顺序，这个顺序由各层之间的依赖关系决定。首先，全局设备枚举（`DEVICES`）在编译时以 `const fn` 构造，不需要运行时初始化。然后，PnP 设备列表和驱动注册表同样以 `const fn` 构造。devtmpfs 在 VFS 初始化时挂载到 `/dev`，挂载时自动创建 `null` 和 `zero` 两个内置字符设备节点。devtmpfs 挂载完成后，注册 PnP 的 devtmpfs 回调，此后 PnP 框架注册的功能设备会自动出现在 `/dev` 目录中。

设备的初始化存在两条路径，其区别在于设备是否参与 PnP 框架的生命周期管理。第一条路径针对启动阶段的静态设备，这些设备在固件描述解析期间被直接识别和初始化，其驱动实例由启动代码直接构造，不经过 PnP 框架的"发现-匹配-探测"流程。第二条路径面向可枚举总线上的动态设备——PCI、USB 等总线在扫描硬件时为每个发现的设备创建 PnP 设备对象，由 PnP 框架统一管理其生命周期。之所以存在这种双路径设计，是因为启动阶段的核心设备（如串口控制台）通常是静态不可热插拔的设备，而这些设备又给我们的内核提供了核心功能支撑，因此最好在 PnP 框架和 devtmpfs 就绪之前就完成初始化；可枚举总线上的设备必须通过动态发现和匹配来处理，而发现和处理的能力有时又必须基于那些静态设备。

静态设备的初始化以 DTB 启动路径下的 UART 串口为例。DTB 解析器在遍历设备树节点时，通过节点名称（而非 `compatible` 属性）识别串口设备，当节点的基本名称为 `serial` 时，解析器从该节点的 `reg` 属性中提取 MMIO 基地址，直接构造 `Uart16550` 驱动实例并完成硬件初始化。随后，解析器将驱动实例包装为 `CharDevice` 对象，以 MMIO 地址作为固件名称，推入全局设备枚举的字符设备子列表。整个过程不涉及 PnP 设备对象的创建，也不涉及驱动匹配，因为驱动类型在编译时已确定，节点名称只是触发条件。这些静态设备在注册时尚未绑定到 devtmpfs（因为 devtmpfs 可能尚未挂载），它们会在 devtmpfs 就绪后通过一个专门的绑定阶段被批量绑定到 `/dev` 目录。以下代码展示了这一流程的核心逻辑：

```c
void register_dtb_serial_node(dtb_node *node) {
    if (node->base_name != "serial") return;
    uint64_t mmio_base = dtb_read_reg(node);
    uart16550 *uart = uart16550_new(mmio_base, DEFAULT_CLOCK, DEFAULT_BAUD);
    char_device *dev = char_device_new(NS16550, node->name, uart);
    devices_push_char(dev);
}
```

PnP 设备的初始化以 PCI 总线上的 VirtIO 块设备为例。PCI 总线层在 devtmpfs 回调注册完成后启动扫描，遍历 PCI 配置空间中的每个功能，为每个有效的 PCI 功能创建一个 PnP 设备对象，将其 vendor ID、device ID 和 class code 设置为硬件身份标识，然后注册到 PnP 框架的全局设备列表。注册完成后，PnP 框架立即尝试为该设备匹配驱动——遍历已注册的驱动列表，调用每个驱动的 `matches` 方法，检查驱动是否支持该设备的硬件身份。当 `VirtioPciBlkDriver` 的 `matches` 方法确认设备的 vendor ID 和 device ID 与 VirtIO 块设备规范匹配时，PnP 框架将设备状态从 `Discovered` 转换为 `Probing`，调用驱动的 `probe` 方法。`probe` 方法映射设备的 BAR 空间，初始化 VirtIO 传输层，协商设备特性，创建请求队列，然后将初始化完成的块设备通过 PnP 设备的 `register_block_function` 方法注册。注册过程采用事务式策略：先将块设备推入全局设备枚举，再通过 devtmpfs 回调创建设备节点；若任一步骤失败，已完成的步骤将被回滚。注册成功后，设备状态转换为 `Bound`，用户程序即可通过 `/dev` 目录中的设备节点访问该块设备。

这个初始化流程的设计确保了每一层在初始化时，它所依赖的下层已经就绪。全局设备枚举不需要运行时初始化，PnP 框架不需要运行时初始化，devtmpfs 在 VFS 初始化时挂载，PnP 回调在 devtmpfs 挂载后注册，总线扫描在回调注册后进行。这种严格的初始化顺序避免了使用未初始化服务的风险，也使得初始化过程是确定性的，不依赖于设备的发现顺序或驱动的注册顺序。

== POSIX 兼容性桥梁

设备模型与 POSIX 之间的桥梁由 devtmpfs 构建。devtmpfs 是一个特殊的文件系统，将设备对象映射为文件系统中的设备节点，使用户程序可以通过 POSIX 文件接口访问设备。devtmpfs 的设计遵循一个核心原则：设备节点的 inode 直接持有设备对象引用，而非设备名称字符串。`open` 操作不需要二次查找，因为设备对象在绑定阶段就已经解析完毕，运行时可以直接调用。

相较于传统的"设备号查找"方式，这种做法带来了一个显著的优势，即消除了设备号到驱动的间接映射。在传统的 Unix 实现中，`open("/dev/ttyS0")` 时，内核首先从 inode 中读取主次设备号，然后在全局的 `cdevsw` 表中查找对应的驱动操作表。这种间接映射存在两个问题：其一，主次设备号的分配需要全局协调，不同子系统可能使用相同的设备号；其二，`cdevsw` 表是一个全局的、固定大小的数组，增加了设备注册的复杂度。devtmpfs 的设计绕过了这个中间层，让 inode 直接持有设备对象的引用，`open` 时直接从 inode 中获取设备对象，无需通过设备号查找驱动。

=== 字符设备的适配

当用户程序对 devtmpfs 中的字符设备节点调用 `open` 时，devtmpfs 的 `open` 操作直接从 inode 中取出 `CharDevice` 对象，构造一个 `CharDevAdapter` 作为 `FileOps`。`CharDevAdapter` 将 VFS 的 `FileOps` 方法翻译为 `CharDevice` 的方法调用，`read` 翻译为字符设备的非阻塞读，`write` 翻译为字符设备的非阻塞写，`ioctl` 通过向下转型恢复具体驱动类型后调用 `DriverControl::control`。

=== 块设备的适配

块设备节点的适配流程与字符设备类似，但更加复杂。devtmpfs 在创建块设备节点时，为该 inode 绑定一个 `DevBlockOps` 对象。`open` 时构造 `BlockDevAdapter`，将 VFS 层的文件操作翻译为块设备驱动的 I/O 操作。但块设备的适配比字符设备更加复杂，因为 VFS 层的 `read`/`write` 是同步的、面向字节的，而块设备驱动的 `submit` 是异步的、面向扇区的。`BlockDevAdapter` 需要将字节范围的读写请求转换为扇区对齐的块设备请求，提交给块设备驱动，然后阻塞等待完成回调。这种同步到异步的桥接是必要的，因为 POSIX 的文件接口是同步的，用户程序期望 `read` 在数据可用时返回，而不是在请求提交后立即返回。

=== ioctl 的翻译路径

`ioctl` 的翻译是适配层中最复杂的部分。POSIX 的 `ioctl` 系统调用接受一个整数命令码和一个 `void*` 参数，驱动根据命令码解释参数的语义。我们的设备模型使用类型安全的 `DriverControl` 接口，不接受整数命令码和原始指针。适配层需要将 POSIX 的 `ioctl` 命令翻译为类型安全的控制请求。以串口设备为例，当用户程序调用 `ioctl(fd, TCSETS, &termios)` 时，VFS 层的 `CharDevAdapter::ioctl` 接收到命令码 `TCSETS` 和用户态指针。适配层首先通过 `as_any` 将 `dyn CharDriver` 向下转型为具体的 `Uart16550` 类型，然后从 `termios` 结构体中提取波特率参数，构造 `UartRequest::SetBaudRate` 请求，调用 `Uart16550::control` 方法。

`ioctl` 的翻译路径中存在一个类型安全的"窄门"。在通用路径上，VFS 层只能通过 `dyn CharDriver` 访问设备，此时无法调用设备特定的控制命令，因为 `DriverControl` 的关联类型在类型擦除后不可见。只有通过 `as_any` 向下转型恢复具体驱动类型后，才能调用设备特定的控制命令。这种设计在通用性和类型安全之间取得了平衡，通用路径保证了所有字符设备都可以用同一种方式访问（读写和刷新），特定路径保证了设备控制命令的类型安全性。

这种分层翻译的设计使得 POSIX 兼容性可以在不修改设备模型的情况下演进。如果未来需要支持新的 `ioctl` 命令，只需要修改设备适配层的翻译逻辑，不需要修改字符设备或块设备的接口定义。如果未来需要替换 `ioctl` 为新的控制接口（例如 `io_uring` 的命令提交机制），只需要修改 VFS 层的适配器，不需要修改设备模型层的任何代码。这种演进能力是分层设计的核心价值，每一层都可以独立演化，而不影响其他层。

== 全局设备枚举

全局设备枚举（`DeviceList`）是设备模型的顶层注册中心，它管理着系统中所有已注册的字符设备和块设备对象。全局设备枚举由字符设备子列表和块设备子列表组成，每个子列表提供添加、查找、移除和遍历操作。全局设备枚举不维护设备号分配或主次设备号到驱动的映射，这些职责属于 devtmpfs 层，而不是设备模型层。这种分离是有意为之的，设备号是 POSIX 的概念，它只在用户程序通过 `/dev` 访问设备时才有意义；内核内部的其他子系统通过全局设备枚举按固件名称查找设备，不需要设备号。

全局设备枚举的查找操作按固件名称进行，返回设备的共享句柄。固件名称是设备在注册时分配的唯一标识，由注册设备的代码在创建时生成。例如，DTB 静态串口的固件名称是其 MMIO 基地址的十六进制表示，PCI 设备的固件名称可能是 BDF（总线-设备-功能）编号的组合，USB 设备的固件名称可能是总线编号和设备地址的组合。固件名称的生成策略由各注册路径自行决定，全局设备枚举只负责按名称查找，不关心名称的含义。

全局设备枚举的列表操作返回快照而非迭代器，这是为了避免在遍历期间持有注册表锁。如果遍历期间持有锁，调用方在遍历体内就不能调用注册表的其他方法（如 `push` 和 `remove`），否则会导致死锁。快照方式先获取一次快照（内存分配在锁外进行），然后在快照上遍历，遍历期间不持有锁，因此可以在遍历体内安全地调用注册表的其他方法。这种设计在一致性和并发性之间取得了平衡，快照不保证反映最新的注册状态，但避免了锁持有时间过长的问题。

== 工程设计总结

设备模型的设计围绕着硬件的异构性与软件对统一接口的需求之间的矛盾这一核心矛盾展开。这个矛盾在操作系统设计中普遍存在，比如说内存管理面对的是同构资源（物理页帧），我们可以选择使用单一抽象覆盖，然而设备管理面对的是异构资源（流式设备、块设备），在这时，任何单一抽象就都无法同时适合所有设备类型了。我们的解决方案是"双轨抽象"：字符设备覆盖流式设备，块设备覆盖随机访问设备，两者各自提供最适合自己设备类型的接口。

双轨抽象相较于传统的"统一设备"抽象（如 Windows 的 `DEVICE_OBJECT` 或微内核的统一端口模型），最大的优势在于接口的精确性。字符设备的非阻塞读写和阻塞刷新的组合，精确匹配了流式设备的行为模式，因为流式数据是实时产生的，调用方需要灵活控制阻塞语义。块设备的异步提交和完成回调的组合，精确匹配了块设备的行为模式，因为 I/O 操作是延迟敏感的，调用方需要充分利用硬件的并行处理能力。如果将这两种接口强行统一，结果要么是字符设备被迫使用异步提交（增加不必要的复杂度），要么是块设备被迫使用同步读写（丧失异步 I/O 的性能优势）。

字符设备接口中阻塞与非阻塞的拆分，体现了"接口规定语义、实现选择策略"的设计原则。传统 Unix 通过 `O_NONBLOCK` 标志在同一个接口上切换阻塞语义，这种做法将阻塞策略的决策权交给了调用方，但同时也将"循环写入加超时逃生"的负担加给了调用方。我们的设计将阻塞和非阻塞拆分为不同的方法，并为阻塞方法提供正确的默认实现，使调用方可以直接使用最适合自己需求的接口，无需自己组装阻塞逻辑。这种设计减少了调用方的出错可能性，也使得驱动的行为更加可预测。

PnP 框架的核心价值在于统一了设备管理的共性逻辑。在 PnP 框架出现之前，每种总线都需要自己实现设备发现、驱动匹配、状态管理和热插拔处理，这些逻辑虽然解决的问题相似，但实现方式各不相同，导致大量代码重复和行为不一致。PnP 框架将这些共性逻辑抽取出来，使新总线的接入只需实现设备发现逻辑，而不需要重新实现驱动匹配和热插拔处理。这种可扩展性不仅减少了代码量，更重要的是减少了出错的可能性，因为共性逻辑只需正确实现一次，所有总线都可以共享。

驱动控制接口的类型安全设计相较于传统 `ioctl` 接口，最大的优势在于将运行时错误前移到编译期。在内核开发中，运行时错误往往意味着系统崩溃或安全漏洞，而编译期错误只需修改代码重新编译。类型安全的控制接口通过关联类型将请求、响应和错误三者绑定在一起，使编译器可以在编译期验证所有控制命令的参数正确性。同时，`as_any` 方法提供的向下转型能力使得类型安全的控制接口不会丧失通用性，调用方可以在需要时恢复具体驱动类型，调用设备特定的控制命令。

POSIX 兼容性的分层实现体现了设备模型与文件系统之间的清晰边界。字符设备和块设备抽象是纯粹的内核内部接口，不包含任何 POSIX 语义；POSIX 语义由 devtmpfs 和 VFS 层在设备抽象之上实现。这种分层设计使得设备模型可以保持简洁和纯粹，而 POSIX 兼容性可以在不修改设备模型的情况下演进。同时，设备模型也可以在不依赖 POSIX 语义的场景下独立使用，比如内核内部的网络协议栈可以直接使用块设备接口读写数据，而不需要通过 devtmpfs 和文件描述符。这种独立性在内核开发的早期阶段尤为重要，因为在文件系统尚未就绪时，内核已经可以通过设备模型访问硬件。

热插拔处理的分阶段移除流程是设备模型可靠性的关键保障。流程的核心原则是"先阻止、再排空、后释放"，即先阻止新的访问和 I/O，再排空已有的 I/O，最后释放资源。这个原则消除了移除过程中的竞态条件：在资源释放之前，所有可能访问这些资源的路径都已被切断，因此不会出现访问已释放内存或调用已卸载驱动方法的情况。这种设计虽然增加了移除流程的延迟，但保证了移除过程的安全性，而安全性在内核开发中永远优先于性能。

回顾整个设备模型的设计，可以发现一个贯穿始终的主题，那就是"分而治之"。字符设备与块设备的双轨划分，让流式访问和块访问各得其所；PnP 框架从总线扫描逻辑中独立出来，使生命周期管理不再与硬件发现纠缠在一起；功能层与 devtmpfs 层各司其职，前者专注 I/O 操作，后者承担 POSIX 兼容性；驱动控制接口与通用读写接口的拆分，让设备特定命令和通用数据传输互不干扰；热插拔的分阶段移除，则将阻止、排空和释放三个动作解耦为可独立验证的步骤。每一次拆分都降低了局部的认知负担，使被拆分的两个部分可以独立演化和优化，而不需要理解对方的内部逻辑。这种"分而治之"的设计哲学与内存管理子系统中的分层递进一脉相承，它不是偶然的巧合，而是操作系统设计中应对复杂性的基本策略。
