#import "../config.typ": project-name
#import "../styles/diagram.typ": flow-arrow, flow-node, layer-card
#import "../styles/figure.typ": continued-table, figure-caption, pseudo-sample
#import "../styles/tokens.typ": handoff-fill, soft-fill, stable-fill, warm-fill

= 第四章 虚拟文件系统与 POSIX 兼容层

操作系统内核的各个子系统，无论是设备模型、内存管理还是进程调度，各自维护着复杂的内部状态，但用户程序不可能也不应该直接触及这些内部表示。用户程序需要的是一套稳定的、语义明确的接口，使得它们可以用统一的方式访问异构的内核资源。虚拟文件系统（VFS）正是承担这一职责的中间层，它将内核内部的设备抽象、内存对象和磁盘文件统一为 POSIX 文件接口，使得用户程序通过 `open` 打开路径名、通过 `read` 和 `write` 传输数据、通过 `close` 释放资源，而无需关心底层资源的物理形态。

VFS 的意义远不止于"为设备提供文件接口"。之所以将 VFS 定位为整个内核的 POSIX 兼容层，是因为它不仅将块设备映射为可读写的文件节点，还将进程运行时状态映射为 procfs 中的虚拟文件、将共享内存区域映射为 tmpfs 中的匿名文件、将进程间通信的字节流映射为管道文件。这种"一切皆文件"的范式并非单纯的接口约定，而是一种架构原则，它要求所有向用户空间暴露的资源都必须经由文件接口访问，不允许存在绕过 VFS 的特殊通道。统一性带来的收益是显著的：用户程序只需掌握一套接口即可访问所有资源，Shell 脚本可以通过重定向和管道自由组合不同类型的资源，调试工具可以通过读取 `/proc` 下的文件获取系统状态而无需专用的系统调用。

然而，将所有资源统一为文件接口也意味着某些不适合文件模型的资源需要被适配进文件的框架。设备控制命令的语义与文件的顺序读写截然不同，但 POSIX 仍然通过文件描述符传递 `ioctl` 命令；网络套接字的连接建立和接受操作也不符合文件的读写模型，但 POSIX 仍然通过 `socket` 系统调用返回文件描述符。这些适配是统一性的代价，但相比于为每种资源设计独立的接口，统一性带来的简化和组合能力远远超过了适配的开销。Shell 的管道和重定向之所以能够工作，正是因为所有资源都通过文件描述符访问；如果每种资源都有自己的接口，Shell 就无法用统一的方式组合它们。

== 索引节点与命名空间的分离

索引节点（Inode）是 VFS 中最核心的数据结构，它代表文件系统中一个具体的对象，可以是普通文件、目录、符号链接、字符设备节点、块设备节点、FIFO 管道或 Unix 域套接字。Inode 与文件名无关：同一个 Inode 可以被多个目录项通过硬链接引用，但底层的数据和元数据只有一份。这种设计源自 Unix 文件系统的经典概念，它将文件的标识（Inode 编号）与文件的命名（目录项）分离，使得文件可以在不改变标识的情况下被重命名、移动或拥有多个名称。

这种分离之所以重要，是因为它解耦了"文件是什么"和"文件叫什么"两个正交的问题。文件的标识由 Inode 编号决定，只要编号不变，文件的身份就不变，无论它被移动到哪个目录、被赋予什么名字。文件的命名由目录项决定，目录项将一个名称映射到一个 Inode，这个映射可以随时被创建、删除或替换。如果将标识和命名耦合在一起，重命名操作就需要修改所有引用该文件的路径，硬链接就不可能实现，`unlink-but-open` 语义也无法保证，而这些恰恰是 POSIX 文件系统语义的基石。

Unix 之所以将 Inode 与目录项分离，根源在于磁盘存储的物理约束。早期的 Unix 文件系统使用磁盘上的固定大小数组存储 Inode，每个 Inode 通过整数编号唯一标识。目录项只是将名称映射到 Inode 编号的记录。这种设计使得硬链接的实现变得自然，多个目录项可以指向同一个 Inode 编号而不需要复制文件数据。同时，`unlink` 操作只需要删除目录项而不需要立即释放 Inode 和数据块，因为其他目录项可能仍然指向同一个 Inode。这种"引用计数"式的回收机制，正是 `unlink-but-open` 语义的基础：文件被删除后，只要仍有打开的文件描述符指向它，数据就不会被释放。

=== 不可变字段与可变字段的拆分

Inode 的字段按照可变性划分为两类。不可变字段在 Inode 创建时确定，此后永远不会改变，包括全局唯一标识符、文件类型、设备号和 I/O 块大小。可变字段被聚合到一个元数据结构体中并通过自旋锁保护，包括文件大小、硬链接计数、权限位、所有者、时间戳和已分配块数。其中，文件大小和硬链接计数还被同步镜像到原子字段，供热路径无锁读取。

之所以将字段分为不可变和可变两类，是因为在 VFS 的热路径中存在大量只读访问。路径解析需要频繁检查文件类型以判断是否为目录从而决定能否继续向下查找，`lseek(SEEK_END)` 需要读取文件大小，`O_APPEND` 写入需要读取文件大小以确定写入位置。如果所有字段都由同一把锁保护，这些高频只读操作就必须与低频写操作竞争同一把锁，导致不必要的阻塞。拆分之后，不可变字段可以在任意时刻无锁读取；可变字段中最频繁被访问的文件大小和硬链接计数通过原子镜像提供了无锁快速路径，只有需要精确一致性快照的场景（如 `stat` 系统调用需要与权限位一致的时间戳）才必须获取锁。

原子镜像的更新策略需要保证与锁保护的主字段一致。每次修改元数据中的 `size` 或 `nlink` 时，必须在持锁期间同步更新对应的原子字段。读取时，如果只需要近似值（如 `lseek(SEEK_END)` 只需要一个足够新的文件大小），可以直接读取原子字段；如果需要精确值则必须获取锁读取元数据。这种"原子镜像加锁保护主字段"的双轨策略，在一致性和性能之间取得了恰当的平衡。

以下代码展示了 Inode 的核心数据结构设计，体现了不可变字段与可变字段的拆分策略以及原子镜像的双轨读取模式。

#pseudo-sample("4-1", [Inode 数据结构与字段拆分], kind: "代码")[
  ```c
  struct InodeId {
      u32 fs_id;     // 文件系统实例标识
      u64 ino;       // 文件系统内唯一编号
  };

  struct InodeMeta {
      u64 size;      // 文件大小
      u32 nlink;     // 硬链接计数
      u16 mode;      // 权限位
      u32 uid, gid;  // 所有者
      Timespec atime, mtime, ctime;  // 时间戳
      u64 blocks;    // 已分配块数
  };

  struct Inode {
      // 不可变字段：创建后永不改变，无锁读取
      const InodeId id;
      const FileType kind;
      const DevId rdev;
      const u32 blksize;

      // 可变字段：自旋锁保护
      Spinlock meta_lock;
      InodeMeta meta;

      // 原子镜像：热路径无锁快速读取
      atomic_u64 cached_size;
      atomic_u32 cached_nlink;

      // 生命周期状态
      atomic_u8 lifecycle;  // LIVE / ORPHANED / EVICTED

      // 操作接口与反向引用
      const InodeOps* ops;
      Weak<Superblock> superblock;
  };
  ```
]

=== 复合标识设计

Inode 的全局唯一标识由文件系统标识符和文件系统内的 Inode 编号两个字段复合而成。文件系统标识符在挂载时分配，全局唯一；Inode 编号由文件系统驱动分配，在同一个文件系统内唯一。两者组合在一起，形成了跨文件系统唯一的 Inode 标识。

之所以采用复合标识而非单一递增整数，是因为 VFS 需要同时支持多个文件系统实例。如果使用单一递增整数，就需要一个全局的 Inode 编号分配器，所有文件系统驱动在创建 Inode 时都需要访问这个分配器，形成全局竞争点。复合标识将编号分配的责任下放到每个文件系统驱动，驱动可以根据自己的内部结构选择最合适的编号方案，比如磁盘文件系统使用磁盘上的 Inode 编号，内存文件系统使用地址的哈希值，设备文件系统使用设备类型的编码。这种去中心化的编号方案消除了全局竞争点，也使得 Inode 编号与文件系统驱动的内部结构保持一致。

=== 共享所有权与两阶段回收

Inode 通过引用计数实现共享所有权。内核中的多个子系统会同时持有对同一 Inode 的引用：目录项缓存中的正向条目持有强引用，超级块的 Inode 缓存持有弱引用，打开的文件对象也持有强引用。这种设计确保了只要有任何一方仍在使用某个 Inode，它就不会被释放。超级块的 Inode 缓存之所以使用弱引用，是为了打破"目录项 → Inode → 超级块 → Inode"的循环引用链，避免内存泄漏。弱引用可以加速 Inode 的查找，因为如果弱引用仍然有效，可以直接升级为强引用，但弱引用不阻止 Inode 在所有强引用释放后被回收。

Inode 的回收采用"两阶段回收"策略。当文件被 `unlink` 且硬链接计数降为零时，VFS 先将该 Inode 从命名空间和缓存中摘除并标记为"孤立"；真正的资源释放则延迟到最后一个强引用消失时由析构器触发。两阶段回收的核心动机是保证 `unlink-but-open` 语义的正确性。在 POSIX 模型中，`unlink` 一个仍然被打开的文件是合法的，`unlink` 之后文件从目录树中消失，但已打开的文件描述符仍然可以正常读写，直到最后一个描述符被关闭后文件才真正消失。如果 `unlink` 时立即释放资源，后续的读写操作将访问已释放的内存。两阶段回收将"从命名空间中消失"和"释放资源"解耦，保证了在资源释放之前所有可能访问这些资源的路径都已经被切断。

这种设计与设备模型中 PnP 设备的"先标记后回收"策略一脉相承。PnP 设备在移除时先标记为不可用以阻止新的 I/O，再排空已有的 I/O，最后释放资源。Inode 在 `unlink` 后先从命名空间中摘除以阻止新的打开操作，再等待所有打开的文件描述符关闭，最后释放资源。两者遵循同一个原则：在资源释放之前，所有可能访问这些资源的路径都已经被切断。

以下代码展示了两阶段回收的完整流程，从 `unlink` 摘除命名空间到最终资源释放的生命周期管理。

#pseudo-sample("4-2", [Inode 两阶段回收流程], kind: "代码")[
  ```c
  // 阶段一：unlink 时从命名空间摘除
  int vfs_unlink(Dentry* parent, const char* name) {
      Dentry* child = dcache_lookup(parent, name);
      Inode* inode = child->inode;

      // 调用文件系统驱动执行实际删除
      int err = parent->inode->ops->unlink(parent->inode, name, inode);
      if (err) return err;

      // 递减硬链接计数（原子镜像同步更新）
      lock(&inode->meta_lock);
      inode->meta.nlink--;
      atomic_store(&inode->cached_nlink, inode->meta.nlink);
      unlock(&inode->meta_lock);

      // 从 Dentry 缓存中移除
      dcache_invalidate(child);

      // 如果 nlink 降为零，标记为孤立
      if (atomic_load(&inode->cached_nlink) == 0) {
          atomic_store(&inode->lifecycle, ORPHANED);
          // 从超级块 Inode 缓存中移除
          inode_cache_remove(inode->superblock, inode->id);
      }

      return 0;
      // 注意：此时 Inode 可能仍被打开的 File 持有强引用
      // 资源不会立即释放
  }

  // 阶段二：最后一个强引用释放时触发
  void inode_drop(Inode* inode) {
      if (atomic_load(&inode->lifecycle) == ORPHANED) {
          atomic_store(&inode->lifecycle, EVICTED);
          // 调用驱动释放磁盘资源（如数据块、磁盘 Inode）
          inode->ops->evict(inode);
      }
      free(inode);
  }
  ```
]

=== 文件系统驱动接口

文件系统驱动需要实现的核心接口定义了 VFS 可以对 Inode 执行的所有操作，这些操作按照语义可以分为三类。命名空间操作包括查找、创建、删除目录项和重命名，这些操作作用于目录 Inode，管理目录项的生命周期。数据操作包括读取、写入和截断，这些操作作用于普通文件 Inode，管理文件数据的传输。元数据操作包括权限修改、时间戳更新和持久化同步，这些操作管理 Inode 的属性。

接口的默认实现为大多数方法返回"不支持"错误，驱动只需要覆盖自己支持的方法。这种默认实现策略降低了驱动的实现负担，驱动不需要为不支持的操作编写样板代码。同时，默认实现也使得 VFS 层可以在不修改驱动代码的情况下添加新的操作方法，已有的驱动不需要任何修改即可继续工作。

接口中有一个特殊的方法允许驱动在文件被打开时返回自定义的数据操作实现。对于普通文件，打开操作通常返回一个通用的文件读写适配器；对于设备节点，打开操作返回设备适配器；对于管道，打开操作返回管道的读写端。这种"打开时绑定操作实现"的策略使得同一个 Inode 可以根据打开方式的不同提供不同的操作接口。

== 目录项与路径缓存

目录项（Dentry）代表命名空间中的一个位置，它将"某个父目录下的某个名字"与最终的 Inode 关联起来，从而使路径字符串能够被逐分量解析为底层对象。Dentry 的核心意义在于把"名字"与"对象"分离。Inode 描述的是文件本体，多个硬链接可以共享同一个 Inode；而 Dentry 描述的是命名空间中的一条命名关系，它天然依赖父目录和名字，因此同一个 Inode 可以被多个不同的 Dentry 指向。

以下代码展示了 Dentry 的核心数据结构，体现了状态机、父子引用和短名称内联优化的设计。

#pseudo-sample("4-3", [Dentry 数据结构与状态机], kind: "代码")[
  ```c
  // Dentry 状态
  enum DentryState {
      POSITIVE,    // 名称存在，指向有效 Inode
      NEGATIVE,    // 名称不存在（缓存失败查找）
      INVALID,     // 缓存过期，需重新验证
  };

  // 短名称内联存储（避免堆分配）
  #define INLINE_NAME_MAX 15
  struct SmallStr {
      u8 len;
      union {
          char inline_buf[INLINE_NAME_MAX];  // 短名称直接内联
          char* heap_ptr;                     // 长名称堆分配
      };
  };

  struct Dentry {
      SmallStr name;                // 路径分量名称
      Arc<Dentry> parent;           // 父目录（强引用，保证祖先存活）
      Arc<Inode> inode;             // 关联的 Inode（正向时非空）
      atomic_u8 state;              // 无锁状态读取
      Spinlock<DentryMeta> meta;    // 可变元数据（rename 时修改）
  };

  // 分片哈希表：全局 Dentry 缓存
  #define N_SHARDS 16
  struct DentryCache {
      struct Shard {
          Spinlock lock;
          HashMap<(uintptr_t parent, SmallStr name), Arc<Dentry>> entries;
          u32 negative_count;       // 负向条目计数
          u32 total_count;          // 总条目计数
      } shards[N_SHARDS];
  };

  // 分片选择：FNV-1a 哈希
  u32 shard_index(Dentry* parent, const char* name) {
      u64 hash = fnv1a(parent, name);
      return hash % N_SHARDS;
  }
  ```
]

=== 正向缓存与负向缓存

Dentry 有三种状态。正向 Dentry 表示某个名称当前存在并指向一个 Inode，这是最常见的状态。负向 Dentry 记录"这个名字在该父目录下当前不存在"，它缓存了查找失败的结果。失效 Dentry 表示缓存的内容可能已经过时，需要重新验证。

负向缓存对于大量失败查找的场景至关重要。Shell 在执行命令时会依次探测 PATH 环境变量中的每个目录，其中大多数路径不存在。如果没有负向缓存，每次失败查找都需要进入底层文件系统驱动甚至触发磁盘 I/O。负向 Dentry 将失败查找的结果缓存起来，后续的查找可以直接从缓存中返回"不存在"而不需要进入文件系统驱动。

然而负向 Dentry 不能永久可信。一旦父目录发生创建类写入（如创建文件或目录），旧的"不存在"结论就可能失效。因此，当父目录的创建操作成功后，VFS 会将该父目录下的所有负向 Dentry 标记为失效，后续的查找需要重新进入文件系统驱动验证。这种"写时失效"的策略在缓存一致性和查找性能之间找到了一个合适的平衡点，写操作的额外开销很小，而读操作可以从负向缓存中获得显著的加速。

=== 分片哈希表与并发控制

Dentry 缓存是一个全局的分片哈希表，它将（父 Dentry 指针，名称）映射到子 Dentry。分片哈希表将全局缓存拆分为多个独立的分片，每个分片有自己的自旋锁。查找时，根据父 Dentry 指针和名称的哈希值选择分片，只需要获取该分片的锁。这种设计使得不同目录、不同名称的查找大概率落入不同分片从而并行执行，避免了单一全局锁在高并发场景下的扩展性瓶颈。

分片数量的选择需要在并发度和内存开销之间权衡。分片过少则锁竞争仍然严重，分片过多则每个分片的平均条目数过低，哈希表的内存开销增大。当前的设计使用固定数量的分片，这个数量在当前处理器核心数下提供了足够的并发度，同时每个分片的平均条目数也足够大，不会造成过多的内存浪费。

Dentry 缓存还设置了每分片的最大条目数限制，防止缓存无限增长。当某个分片的条目数超过限制时，淘汰策略优先移除负向条目，因为负向条目只是失败查找的缓存，移除后最多导致一次额外的文件系统驱动调用，而不会丢失任何数据。正向条目的淘汰意味着释放对 Inode 的强引用，如果该 Inode 的硬链接计数为零且没有打开的文件持有引用，Inode 将被两阶段回收策略最终释放。有界约束保证了 Dentry 缓存不会在长时间运行的系统中无限膨胀。

=== 目录树的所有权结构

Dentry 通过引用计数形成一棵内存中的目录树。子节点持有父节点的强引用，使得只要某个路径下游节点仍在使用，其祖先目录就不会被过早释放。根节点的父引用为空，避免通过自引用制造永久性的引用环。进程可见根的约束由 VFS 上下文维护，因此"到达根目录时停止向上回溯"是路径解析逻辑的职责，而不是通过特殊的自环 Dentry 来隐式表达。

子节点持有父节点的强引用而非弱引用，是为了保证路径解析的稳定性。当路径解析从根目录向下遍历时，每一步都需要访问当前 Dentry 的父目录。如果子节点持有父节点的弱引用，父节点可能在任何时候因缓存淘汰被回收，导致路径解析失败。强引用保证了只要子节点存在父节点就一定存在，路径解析不需要处理父节点被回收的异常情况。这种设计也意味着 Dentry 树的回收是自底向上的，只有当所有子节点都被回收后，父节点才可能被回收。对于深度嵌套的目录结构，缓存的有界约束会定期淘汰最久未访问的条目，防止目录树无限增长。

短名称优化是 Dentry 缓存的一个重要性能考量。文件系统中的大多数路径分量都是短字符串，像 `bin`、`lib`、`etc`、`src` 这类名称很少超过十几个字节。如果每个名称都在堆上分配独立的缓冲区，内存分配器的开销将成为路径解析的瓶颈。Dentry 使用一种内联存储策略：当名称长度不超过阈值时，直接存储在 Dentry 结构体内部，不需要额外的堆分配；只有超过阈值的长名称才使用堆分配。这种优化在保持接口透明的同时，显著减少了内存分配器的压力。

== 打开文件与能力冻结模型

当进程调用 `open` 系统调用时，内核创建一个文件对象并将其编号返回给用户空间。此后进程通过文件描述符编号进行所有 I/O 操作，而不再直接接触路径或 Inode。文件对象是 VFS 能力冻结模型的载体，它在创建时固化了打开标志和调用者凭据，后续的读写调用不再重新检查路径权限，只验证描述符标志是否允许该操作。

=== 能力冻结与 TOCTOU 防御

能力冻结模型是 VFS 安全性的基石。传统的"每次检查权限"模型存在 TOCTOU（Time-of-Check to Time-of-Use）漏洞：攻击者可以在权限检查和实际操作之间修改文件的权限或替换文件指向，从而绕过权限检查。能力冻结模型在打开时就确定了操作的权限边界，后续的操作只能在这个边界内进行，从根本上消除了 TOCTOU 漏洞。

文件对象在创建时固化了打开标志，包括读写模式、追加模式、非阻塞模式等。后续的读写调用只检查描述符标志是否允许该操作，不再重新查询路径权限。例如，如果文件以只读模式打开，后续的写入调用将直接返回错误而不需要检查文件当前的权限位。这种设计不仅提高了安全性，还简化了 I/O 路径上的权限检查逻辑，减少了锁竞争。

文件对象还在创建时固化了调用者的凭据快照。凭据以引用计数指针的形式存储，修改时通过"整体替换"策略更新，即创建一个新的凭据对象然后原子地替换指针。这种设计避免了撕裂写的问题：正在读取旧凭据的路径持有旧对象的引用，不会看到部分更新的字段；新路径读取到新指针后看到的是完整的新凭据。凭据的整体替换策略与 POSIX 的 `setuid` 语义一致，`setuid` 将多个用户 ID 字段同时修改为新值，这本身就是一个原子操作。

=== 数据操作接口与命名空间操作接口的分离

数据操作接口定义了已打开文件的 I/O 行为，包括读取、写入、目录枚举、同步、轮询、设备控制和释放。命名空间操作接口定义了目录管理行为，包括查找、创建、删除和重命名。这两种接口的分离使得文件系统驱动可以独立演化，命名空间操作的修改不会影响数据操作，反之亦然。

这种分离的设计动机在于两类操作的生命周期不同。命名空间操作作用于目录 Inode，与文件的打开状态无关；数据操作作用于已打开的文件，与文件的命名空间位置无关。一个文件可以被移动到另一个目录，但已打开的文件描述符仍然可以正常读写。如果将两种操作合并到同一个接口中，驱动就需要处理命名空间操作和数据操作之间的交互，增加了实现的复杂度。

数据操作接口的释放方法在文件对象的引用计数归零时自动调用，驱动应在此释放所有打开状态。释放方法与 Inode 的资源回收方法的区别在于：前者在每次最终关闭时调用，后者只在 Inode 引用计数降至零时调用一次。这种区分反映了"文件打开状态"和"文件存在状态"是两个不同的概念，一个文件可以存在但没有被打开，也可以被多次打开但只存在一次。

=== 文件偏移量的无锁管理

文件读写偏移量使用原子整数保护，加载和存储操作提供了与自旋锁相同的可见性保证，且无锁开销。多线程并发读写同一描述符时，调用方应使用位置读写接口以避免位置竞争，这与 POSIX 语义一致。位置读写接口接受显式的偏移量参数，不修改文件偏移量，因此可以在无锁条件下安全地并发执行。

文件偏移量使用原子操作而非自旋锁保护，其理由在于偏移量的读写模式是简单的"读取-修改-写入"，不需要保护多个相关字段的原子性。自旋锁在这种场景下是过度同步的，它不仅增加了锁获取和释放的开销，还引入了不必要的锁竞争。原子操作将偏移量的更新简化为一次原子的比较并交换，在没有竞争时只需要一次原子操作，在有竞争时也不需要自旋等待。

== 文件描述符表

文件描述符表将整数编号映射到文件对象，是进程与内核 VFS 之间的接口桥梁。系统调用层持有进程的文件描述符表，通过整数编号找到对应的文件对象后调用 VFS 操作。

以下代码展示了文件对象与文件描述符表的核心数据结构，体现了能力冻结和位图加速分配的设计。

#pseudo-sample("4-4", [文件对象与文件描述符表], kind: "代码")[
  ```c
  // 打开标志：在 open 时冻结
  struct OpenOptions {
      bool readable;
      bool writable;
      bool append;
      bool nonblock;
      bool nofollow;
      bool directory;
  };

  // 文件对象：能力冻结的载体
  struct File {
      Arc<Inode> inode;           // 底层 Inode
      OpenOptions opts;           // 冻结的打开标志
      Arc<Credentials> cred;      // 冻结的调用者凭据
      atomic_u64 pos;             // 文件偏移量（无锁）
      const FileOps* ops;         // 数据操作接口
      Arc<Mount> mount;           // 所在挂载点
  };

  // 文件描述符表
  struct FdTable {
      Spinlock lock;
      struct FdEntry {
          Arc<File> file;
          u32 flags;              // CLOEXEC 等描述符级标志
      }* entries;
      u32 count;                  // 当前条目数
      u32 limit;                  // 软限制
      u32 hard_limit;             // 硬限制
      u64* bitmap;                // 位图：加速最小可用编号查找
  };

  // O(1) 最小可用编号分配
  int fd_alloc(FdTable* table) {
      for (u32 word = 0; word < table->hard_limit / 64; word++) {
          if (table->bitmap[word] != ~0ULL) {
              u32 bit = ctz(~table->bitmap[word]);  // 尾零计数
              u32 fd = word * 64 + bit;
              if (fd >= table->limit) return -EMFILE;
              table->bitmap[word] |= (1ULL << bit);
              return fd;
          }
      }
      return -EMFILE;
  }
  ```
]

=== 编号分配与位图加速

编号分配遵循 POSIX 的"最小可用编号"规则，即 `open` 或 `dup` 分配的编号总是当前最小可用的非负整数。这个规则保证了编号的紧凑性，避免了编号无限增长导致的数组膨胀。实现上使用位图加速最小可用编号的查找：位图的第 i 位为 1 表示编号 i 已被占用，查找最小可用编号时遍历位图的每个机器字并使用尾零计数指令找到第一个 0 位，时间复杂度接近 O(1)。

位图按硬限制一次性分配，而条目数组则按需增长。这种设计在内存效率和查找速度之间找到了平衡，位图的大小由硬限制决定，即使硬限制设为数千，位图也只占用几百字节；条目数组的大小由实际打开的文件数量决定，不会因为硬限制很高而浪费内存。

=== CLOEXEC 与 execve 时的批量关闭

每个文件描述符条目携带描述符级标志，其中最重要的是 CLOEXEC 标志，它表示该描述符在 `execve` 时应该被自动关闭。这个标志的引入是为了解决多线程程序中的文件描述符泄露问题：在 `fork` 和 `exec` 之间，另一个线程可能通过 `dup` 或 `socket` 占用了原本属于旧程序的编号，导致新程序意外继承了不该访问的文件描述符。设置 CLOEXEC 后，`execve` 会自动关闭这些描述符，消除了泄露的风险。

批量关闭的实现采用"锁内提取、锁外释放"的策略。在持锁期间将所有标记了 CLOEXEC 的条目从表中提取出来放入栈上缓冲区，然后释放锁，在锁外批量释放这些条目。之所以不在持锁期间执行释放操作，是因为文件对象的释放可能触发耗时的操作（如等待 I/O 完成），如果在持锁期间执行这些操作，锁的持有时间将不可预测，影响其他线程对文件描述符表的访问。

=== fork 时的描述符表复制

`fork` 时子进程获得父进程文件描述符表的深拷贝。深拷贝复制每个条目的文件对象引用，使得父子进程共享同一个文件对象，这意味着父子进程共享文件偏移量。如果父进程读取了 100 字节，子进程的文件偏移量也前进了 100 字节。这种共享语义是 POSIX 规定的，它使得父子进程可以通过文件描述符协同工作。

当线程创建标志指定共享文件描述符表时，子任务与父任务共享同一个表实例而不是创建副本。共享时一方的打开、关闭和复制操作对另一方可见。这种共享语义在多线程程序中是必要的，因为同一进程的线程应该共享文件描述符表，否则一个线程打开的文件对其他线程不可见，这与 POSIX 的线程语义矛盾。

=== 软限制与硬限制

文件描述符表区分软限制和硬限制。软限制是当前生效的限制，打开和复制操作不能分配超过软限制的编号。硬限制是软限制可以提升到的上限，普通进程可以调整自己的软限制但不能超过硬限制，只有特权进程才能提升硬限制。已经打开的描述符不会因为软限制下调而被关闭，这种设计保证了限制调整不会导致正在运行的程序出现意外的文件描述符关闭。

== 路径解析算法

路径解析是 VFS 中最频繁的操作之一，每次 `open`、`stat`、`execve` 和 `chdir` 都需要将用户提供的路径字符串解析为对应的 Dentry 和挂载点。路径解析的性能直接决定了这些系统调用的响应时间，而 Dentry 缓存是路径解析性能的关键保障。

=== 逐分量解析与缓存查找

路径解析从当前工作目录或根目录开始，逐分量遍历路径字符串。对于每个分量，解析器首先在 Dentry 缓存中查找是否已有该名称的子项。如果缓存命中则直接使用缓存的 Dentry；如果缓存未命中则调用当前 Inode 的查找方法从底层文件系统中查找该名称对应的 Inode，然后创建新的 Dentry 并加入缓存。缓存命中时路径解析不需要进入文件系统驱动也不需要磁盘 I/O，时间复杂度接近 O(1)。

以下代码展示了路径解析的核心逻辑。解析器从起始目录开始逐分量遍历，每一步都检查 Dentry 缓存、挂载点跨越和符号链接展开，最终返回目标 Dentry 和所在的挂载点。

#pseudo-sample("4-5", [路径解析核心逻辑], kind: "代码")[
  ```c
  struct LookupResult {
      Dentry* dentry;
      Mount*  mount;
  };

  LookupResult resolve_path(VfsContext* ctx, const char* path, int flags) {
      Dentry* current;
      Mount*  mnt;

      // 确定起始目录
      if (path[0] == '/') {
          current = ctx->root_dentry;
          mnt = ctx->root_mount;
      } else {
          current = ctx->cwd_dentry;
          mnt = ctx->cwd_mount;
      }

      int symlink_depth = 0;
      char* component = next_component(path);

      while (component != NULL) {
          // 处理 ".." 分量：检查是否到达进程根
          if (strcmp(component, "..") == 0) {
              if (current == ctx->root_dentry)
                  goto next;  // 到达根目录，停止回溯
              // 检查是否需要退出当前挂载
              if (current == mnt->root_dentry) {
                  current = mnt->mountpoint;
                  mnt = mnt->parent;
              }
              current = current->parent;
              goto next;
          }

          // 在 Dentry 缓存中查找子项
          Dentry* child = dcache_lookup(current, component);
          if (child == NULL) {
              // 缓存未命中，调用文件系统驱动
              Inode* inode = current->inode->ops->lookup(current->inode, component);
              child = dentry_create(current, component, inode);
              dcache_insert(child);
          }

          // 检查是否为挂载点
          Mount* child_mnt = lookup_mount(mnt->namespace, child);
          if (child_mnt != NULL) {
              child = child_mnt->root_dentry;
              mnt = child_mnt;
          }

          // 处理符号链接
          if (child->inode != NULL && child->inode->kind == SYMLINK) {
              if (++symlink_depth > MAX_SYMLINK_DEPTH)
                  return ERROR(ELOOP);
              const char* target = child->inode->ops->readlink(child->inode);
              LookupResult link_result = resolve_path(ctx, target, flags);
              child = link_result.dentry;
              mnt = link_result.mount;
          }

          current = child;
      next:
          component = next_component(NULL);
      }

      return (LookupResult){ .dentry = current, .mount = mnt };
  }
  ```
]

=== 符号链接展开与深度限制

符号链接需要递归展开，解析器跟踪链接指向的路径，直到遇到非链接的 Inode 为止。为了防止循环符号链接导致无限递归，解析器维护一个链接展开计数器，当计数器超过阈值时返回错误。符号链接的展开方式取决于链接是绝对路径还是相对路径：绝对路径从根目录开始解析，相对路径从符号链接所在的目录开始解析。这种区分是必要的，因为符号链接的"当前目录"是链接所在的目录而不是进程的当前工作目录。

=== 挂载点跨越

当路径解析到达一个 Dentry 时，解析器需要检查该 Dentry 是否是某个挂载点的根。如果是，解析器需要"进入"被挂载的文件系统，从该挂载的超级块的根 Dentry 继续解析。当解析器沿着 Dentry 树向上回溯（处理 `..` 分量）时，如果当前 Dentry 是某个挂载的根，解析器需要"退出"被挂载的文件系统，回到挂载点所在的父文件系统继续回溯。挂载点的查找通过挂载命名空间的索引实现，给定一个 Dentry 即可在对数时间内找到以该 Dentry 为挂载点的所有挂载。

=== 最后分量的特殊处理

路径解析的最后一个分量需要特殊处理，因为打开操作需要根据打开标志执行不同的逻辑。如果创建标志被设置且最后一个分量不存在，解析器需要调用父目录的创建方法创建新文件。如果排他标志也被设置且文件已存在，解析器返回"已存在"错误。如果截断标志被设置且文件存在且是普通文件，解析器需要将文件大小截断为零。这些标志的组合语义由 VFS 层统一处理，文件系统驱动只需要实现创建和截断方法，不需要理解标志的组合逻辑。这种"VFS 层处理语义、驱动层处理实现"的分工，使得驱动的实现更加简洁，也保证了不同文件系统对同一组标志的行为是一致的。

=== 进程可见根与 chroot 边界

VFS 上下文封装了进程的可见根目录及其所在挂载点。`chroot` 系统调用修改进程的可见根，将进程的文件系统视图限制在某个子树下。路径解析在处理 `..` 时需要检查当前 Dentry 是否是进程的根目录，如果是则停止向上回溯，防止进程逃出 `chroot` 的边界。根目录的判断通过指针比较实现，比字符串比较更高效也避免了路径规范化的问题。

== 挂载管理与命名空间

挂载是将一个文件系统的根目录叠加到目录树某个节点上的操作。挂载之后，访问该节点的路径将进入被挂载文件系统而不是原来的目录内容。挂载管理是 VFS 中最复杂的子系统之一，它需要处理挂载树的维护、挂载点的查找和命名空间的隔离。

=== 挂载树的结构

挂载树是一棵以根挂载为根的有向树。每个挂载节点记录了其超级块、挂载点 Dentry、父挂载和子挂载列表。根挂载的父引用为空，对应文件系统命名空间的根目录。子挂载的父引用指向其挂载点所在的挂载，形成一棵层次分明的挂载树。

以下代码展示了挂载节点与挂载命名空间的数据结构，以及挂载和卸载操作的核心流程。

#pseudo-sample("4-6", [挂载管理数据结构与操作流程], kind: "代码")[
  ```c
  struct Mount {
      Arc<Superblock> superblock;   // 被挂载的文件系统
      Arc<Dentry> mount_root;       // 挂载文件系统的根 Dentry
      Spinlock<MountLocation> location;
      atomic_u32 flags;             // RDONLY, NOSUID, NODEV, NOEXEC...
      atomic_usize open_count;      // 繁忙检测引用计数
  };

  struct MountLocation {
      Arc<Dentry> mountpoint;       // 挂载点所在的 Dentry
      Weak<Mount> parent;           // 父挂载（弱引用避免环）
      Vec<Arc<Mount>> children;     // 子挂载列表
  };

  struct MountNamespace {
      Spinlock<Arc<Mount>> root;    // 根挂载
      Spinlock<Vec<Arc<Mount>>> mounts;  // 所有挂载的平坦索引
  };

  // 挂载操作
  int do_mount(MountNamespace* ns, Dentry* mountpoint,
               const char* fs_type, u32 flags) {
      // 1. 查找文件系统驱动
      FsDriver* driver = fs_registry_lookup(fs_type);
      if (!driver) return -ENODEV;

      // 2. 调用驱动创建超级块
      Superblock* sb = driver->mount(driver, flags);
      if (!sb) return -ENOMEM;

      // 3. 创建挂载节点
      Mount* mnt = mount_create(sb, mountpoint, flags);

      // 4. 在命名空间单锁保护下插入挂载树
      lock(&ns->mounts);
      Mount* parent_mnt = find_mount_at(ns, mountpoint);
      mount_attach(parent_mnt, mnt);
      vec_push(&ns->mounts, mnt);
      unlock(&ns->mounts);

      return 0;
  }

  // 卸载操作
  int do_umount(MountNamespace* ns, Mount* mnt) {
      // 繁忙检测：检查是否有活跃引用
      if (atomic_load(&mnt->open_count) > 0)
          return -EBUSY;

      lock(&ns->mounts);
      // 从父挂载的子列表中移除
      mount_detach(mnt);
      vec_remove(&ns->mounts, mnt);
      unlock(&ns->mounts);

      // 释放超级块（可能触发 sync_fs）
      superblock_put(mnt->superblock);
      return 0;
  }
  ```
]

挂载树与 Dentry 树是两棵独立的树，但它们之间存在映射关系。Dentry 树描述的是文件系统的逻辑结构，每个 Dentry 对应一个目录项；挂载树描述的是文件系统的物理布局，每个挂载对应一个文件系统实例。当路径解析沿着 Dentry 树向下遍历时，每到达一个 Dentry 都需要检查该 Dentry 是否是某个挂载的挂载点，如果是就需要切换到挂载树中对应的子挂载继续解析。

挂载节点还维护了挂载标志，这些标志独立于文件系统本身的属性。同一个文件系统可以在不同的挂载点以不同的标志挂载，例如在一个挂载点以只读方式挂载，在另一个挂载点以读写方式挂载。挂载标志使用原子整数存储，使得只读检查可以在不获取锁的情况下完成。

=== 挂载命名空间与容器隔离

挂载命名空间允许不同进程组看到不同的文件系统视图。每个挂载命名空间拥有独立的挂载树，同一物理设备可以在不同命名空间中以不同方式挂载。容器技术正是利用此机制为每个容器提供独立的根文件系统而不影响宿主机。

挂载命名空间在 `clone` 时根据命名空间标志决定是否创建新的命名空间。如果创建新命名空间，子进程获得父命名空间的挂载树的副本，此后父子命名空间的挂载操作互不影响。如果不创建新命名空间，父子进程共享同一个挂载命名空间，一方的挂载操作对另一方可见。

挂载命名空间的数据结构使用单锁保护，所有对挂载树的修改和读取都需要获取这把锁。单锁设计虽然增加了锁的粒度，但消除了多锁操作导致的死锁风险和不一致窗口。在挂载操作远少于路径解析操作的实际场景中，单锁的性能影响是可以接受的，因为路径解析中的挂载点查找只需要短暂持锁读取索引，而挂载操作虽然需要较长时间持锁但发生频率极低。

=== pivot_root 与容器初始化

`pivot_root` 是容器初始化的关键操作，它将当前命名空间的根挂载替换为新的文件系统。`pivot_root` 的实现需要原子地完成多个步骤：创建新挂载、将旧根挂载的子挂载移动到新挂载下、更新命名空间的根指针。这些步骤必须在同一次锁获取中完成，否则并发的路径解析可能看到中间状态导致解析错误。`pivot_root` 的原子性保证是挂载命名空间单锁设计的直接收益。如果挂载树的各个部分由不同的锁保护，`pivot_root` 就需要同时获取多个锁，不仅增加了死锁的风险，还增加了不一致窗口的长度。

=== 挂载点的繁忙检测

卸载文件系统之前必须确认挂载点上没有活跃的文件描述符或路径引用。如果挂载点仍然繁忙，卸载操作必须失败，否则正在使用该文件系统的进程将访问到已释放的资源。挂载点的繁忙检测通过引用计数器实现，在文件打开和关闭、路径解析进入和离开挂载时相应递增和递减。检测时只需要读取计数器的值，时间复杂度为 O(1)，无需扫描整个进程的文件描述符表。计数器的更新在文件打开和关闭的热路径上增加了一次原子操作，但这个开销远小于扫描文件描述符表的开销。

== 超级块与文件系统实例

超级块是文件系统实例的元数据容器，它记录了文件系统的类型、标识符、块大小、根 Dentry 和 Inode 缓存。每个挂载的文件系统都有一个对应的超级块，超级块在文件系统挂载时创建，在卸载时销毁。超级块的设计将文件系统的"实例状态"与"操作逻辑"分离，实例状态存储在超级块的字段中，操作逻辑由各层接口定义。

=== Inode 缓存与弱引用加速

超级块的 Inode 缓存使用弱引用持有已创建的 Inode。弱引用不阻止 Inode 被回收，当所有强引用都释放后 Inode 会被自动回收。弱引用的作用是加速 Inode 的查找，当 VFS 需要根据 Inode 编号查找 Inode 时，首先检查超级块的 Inode 缓存，如果弱引用仍然有效就可以直接升级为强引用而不需要调用文件系统驱动重新从磁盘读取 Inode。

Inode 缓存使用分片设计以减少锁竞争。多个分片各自持有独立的锁，不同 Inode 编号的查找大概率落入不同分片从而并行执行。分片内部使用有序映射存储弱引用，查找的时间复杂度为 O(log n)。当 Inode 被回收时，其在缓存中的弱引用自动失效，下次查找时发现失效的弱引用会将其清除。这种惰性清理策略避免了在 Inode 回收的热路径上增加额外的缓存维护开销。

=== 文件系统驱动注册

文件系统驱动通过全局注册表注册自己的类型名称和挂载方法。挂载操作时，VFS 根据用户指定的文件系统类型名称在注册表中查找对应的驱动，然后调用驱动的挂载方法创建超级块。驱动的挂载方法负责初始化超级块的所有字段，包括根 Inode 和根 Dentry 的创建。

驱动注册表还维护了驱动的标志信息，标志指示了驱动的特性。例如"无设备"标志表示该文件系统不需要底层块设备（如 tmpfs 和 procfs），"单实例"标志表示该文件系统在整个系统中只能有一个实例（如 procfs），"需要设备"标志表示该文件系统必须有底层块设备（如 ext4）。这些标志使得 VFS 可以在挂载前进行基本的合法性检查，避免将不合法的挂载请求传递给驱动。

== 特殊文件系统

VFS 不仅服务于磁盘文件系统，还服务于多种特殊文件系统。这些特殊文件系统是"一切皆文件"范式的具体体现，它们将内核的各种内部状态以文件的形式暴露给用户空间，使得用户程序可以用统一的文件接口访问原本需要专用系统调用才能获取的信息。

=== devtmpfs 与设备节点的零查找绑定

devtmpfs 是一个特殊的内存文件系统，它将设备对象映射为文件系统中的设备节点。devtmpfs 的设计遵循一个核心原则：设备节点的 Inode 直接持有设备对象引用，而不是设备名称字符串或设备号。当用户程序打开一个设备节点时，devtmpfs 的打开操作直接从 Inode 中取出设备对象并构造适配器作为数据操作接口，不需要通过设备号查找驱动。

这种"零查找绑定"设计相较于传统的"设备号查找"方式消除了设备号到驱动的间接映射。在传统的 Unix 实现中，打开设备节点时内核首先从 Inode 中读取主次设备号，然后在全局的设备表中查找对应的驱动操作表。这种间接映射存在两个问题：主次设备号的分配需要全局协调，全局设备表是一个固定大小的数组增加了设备注册的复杂度。devtmpfs 的设计消除了这种间接映射，让 Inode 直接持有设备对象的引用，打开时直接从 Inode 中获取设备对象。

devtmpfs 与设备模型的交互通过 PnP 框架的回调机制实现。当设备注册时 PnP 框架调用 devtmpfs 的回调创建对应的设备节点；当设备移除时 PnP 框架调用 devtmpfs 的回调删除对应的设备节点。这种回调机制保证了 devtmpfs 中的设备节点与设备模型中的设备对象始终保持一致。

=== procfs 与进程信息的文件化暴露

procfs 将内核的运行时状态以文件的形式暴露给用户空间。系统级信息文件包括内存使用情况、挂载信息、处理器信息和文件系统列表等。procfs 的实现与磁盘文件系统有本质不同：磁盘文件系统的查找操作从磁盘读取目录数据，procfs 的查找操作则根据名称动态生成 Inode；磁盘文件系统的读取操作从磁盘读取文件数据，procfs 的读取操作则在读取时动态格式化内核状态为文本。这种"按需生成"的设计使得 procfs 不需要占用磁盘空间，也保证了其中的信息始终反映内核的最新状态。

=== tmpfs 与内存文件

tmpfs 是一个纯内存文件系统，它将文件数据存储在物理内存中不需要磁盘支持。tmpfs 的核心优势在于速度和灵活性，文件的读写不需要磁盘 I/O，创建和删除不需要磁盘分配和回收。tmpfs 常用于临时文件目录、共享内存对象和早期用户空间的根文件系统。tmpfs 的目录结构使用有序映射管理，查找、创建和删除操作的时间复杂度均为 O(log n)，所有操作都在内存中完成不需要磁盘 I/O。

=== 管道文件系统

管道是 VFS 中一种特殊的文件类型，它提供进程间的字节流通信。管道由 `pipe` 系统调用创建，返回两个文件描述符，一个用于读取，一个用于写入。管道的数据缓冲区是内核内存中的环形缓冲区，写入端将数据追加到缓冲区尾部，读取端从缓冲区头部取出数据。当缓冲区满时写入端阻塞或返回错误，当缓冲区空时读取端阻塞或返回错误。

管道的读写端引用计数用于检测对端是否已关闭。当写入端的读者计数降为零时，后续的写入操作返回"管道断开"错误，符合 POSIX 的语义。当读取端的写者计数降为零时，后续的读取操作返回零（EOF）。管道的轮询实现支持可读和可写事件，使得进程可以使用 `poll` 或 `epoll` 等待管道的状态变化而不需要阻塞在读写操作上。

管道文件系统是一个内核内部的文件系统，它为管道提供 Inode 和超级块。管道文件系统不挂载到任何用户可见的目录，它的存在只是为了给管道的文件对象提供一个合法的 Inode 和挂载点引用。这种设计使得管道的文件对象与普通文件的文件对象具有相同的结构，系统调用层不需要为管道编写特殊的处理逻辑，这正是"一切皆文件"范式在内核内部的体现。

== VFS 与其他子系统的交互

VFS 作为 POSIX 兼容层需要与内核中的多个子系统交互。这些交互通过明确的接口边界实现，每个子系统只依赖 VFS 提供的特定接口而不需要理解 VFS 的内部实现。

=== VFS 与设备模型的交互

VFS 与设备模型的交互通过 devtmpfs 实现。这种交互是单向的，devtmpfs 依赖设备模型提供的设备对象，但设备模型不知道 devtmpfs 的存在。设备模型通过 PnP 框架的回调机制通知 devtmpfs 设备的注册和移除，devtmpfs 根据通知创建或删除设备节点。这种单向依赖的设计与内核的分层原则一致：设备模型层不依赖文件系统层，文件系统层依赖设备模型层。回调机制将依赖关系保持在正确的方向上，devtmpfs 注册回调时依赖 PnP 框架提供的注册接口，PnP 框架不依赖 devtmpfs 的任何代码。

=== VFS 与进程管理的交互

VFS 与进程管理的交互通过 VFS 上下文和文件描述符表实现。进程管理子系统在创建新进程时根据 `clone` 标志决定是否共享 VFS 上下文和文件描述符表。进程退出时文件描述符表中的所有文件被自动关闭。VFS 通过调度器的子系统侧表访问进程的 VFS 状态，而不需要直接引用进程管理子系统的数据结构。这种间接访问使得 VFS 和进程管理可以独立演化，只要侧表接口保持稳定，两个子系统的内部修改不会互相影响。

#figure(caption: figure-caption("图", "4-1", [VFS 分层架构与子系统交互]))[
  #layer-card("POSIX 系统调用层", [open / read / write / close / stat / ioctl / mmap], fill: soft-fill)
  #align(center)[#text(fill: rgb("#2d5f73"))[↓]]
  #layer-card("VFS 核心层", [路径解析、Dentry 缓存、Inode 管理、挂载命名空间、文件描述符表], fill: soft-fill)
  #align(center)[#text(fill: rgb("#2d5f73"))[↓]]
  #layer-card("文件系统驱动层", [ext4 / tmpfs / devtmpfs / procfs / pipefs], fill: warm-fill)
  #align(center)[#text(fill: rgb("#2d5f73"))[↓]]
  #layer-card("块设备 / 内存后端", [磁盘 I/O 调度、页缓存、内存分配], fill: stable-fill)
]

=== VFS 与内存管理的交互

VFS 与内存管理的交互主要体现在两个方面。第一，`mmap` 系统调用将文件的内容映射到进程的地址空间，使得进程可以通过内存访问的方式读写文件数据而不需要显式的 `read`/`write` 系统调用。第二，页缓存作为磁盘文件系统的读写缓冲区，由内存管理子系统管理其物理页帧的分配和回收。这两种交互都通过明确的接口边界实现，VFS 不直接操作页表，内存管理不直接操作文件系统元数据。

== 设计原则总结

VFS 的设计贯穿了几个核心原则。第一是"名字与对象分离"，Dentry 管理名字，Inode 管理对象，两者通过引用关联但各自独立演化。第二是"能力冻结"，文件对象在打开时固化权限边界，后续操作不再重新检查路径权限。第三是"两阶段回收"，资源的命名空间移除和物理释放解耦，保证了 `unlink-but-open` 语义的正确性。第四是"分片并发"，Dentry 缓存和 Inode 缓存都使用分片设计减少锁竞争。第五是"单向依赖"，VFS 依赖设备模型和内存管理，但后者不依赖 VFS，保持了清晰的分层结构。这些原则共同构成了一个既满足 POSIX 语义要求又具备良好扩展性的文件系统抽象层。
