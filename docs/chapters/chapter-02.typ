#import "../config.typ": project-name
#import "../styles/diagram.typ": flow-arrow, flow-node, layer-card
#import "../styles/figure.typ": continued-table, figure-caption, pseudo-sample
#import "../styles/tokens.typ": handoff-fill, soft-fill, stable-fill, warm-fill

= 第二章 内存管理子系统

当硬件将控制权转交给操作系统并建立起最小的运行环境之后，内核的首要任务就是提取硬件的关键信息。一旦获得内存布局信息，便需要立即建立一套可用的内存管理机制。内存是操作系统最基础的资源，没有内存管理，后续的任何子系统都无法运转。因此，内存分配器的初始化是内核启动链路中最早被激活的核心子系统之一。

然而，内存管理的意义远不止于简单地分配和释放内存块。在一个运行着数百个进程的操作系统中，内存是被各方争夺的珍贵资源：进程需要内存来存储代码和数据，文件系统需要内存来缓存元信息，设备驱动需要内存来构建 DMA 缓冲区，网络栈需要内存来存放数据包。如果管理机制设计不当，即使物理内存总量充足，系统也可能因碎片化、分配延迟或内存泄漏而无法正常运行。因此，内存管理子系统的设计质量直接决定了整个操作系统在长期运行中的稳定性和性能表现。

内存管理的本质，是在离散的物理内存之上构造一套连续、可预测、可扩展的抽象模型，使操作系统能够以统一的方式管理这块"既不连续、又必须连续"的资源。所谓"不连续"，是指硬件层面的物理内存可能来自多个不连续的地址区间，其中穿插着预留区域、固件占用和设备映射等复杂情况；所谓"连续"，是指软件层面必须为应用程序提供连续平坦的虚拟地址空间，让它们可以在一个看似无边界的地址范围内自由分配和释放内存。这种物理离散与逻辑连续之间的映射，正是内存管理子系统的核心职能。

从内核启动的最早期到运行期的日常分配，系统对内存管理器的需求截然不同。启动最早期，内核尚无任何动态分配能力，甚至连一个可调用的分配函数都不存在，此时需要一种极其简单的机制来满足最原始的分配需求。而到了运行期，内核需要处理各种大小不一的分配请求，高效回收已释放的内存，避免外部碎片的累积，还要为用户态进程提供独立的地址空间管理。这些需求之间存在天然的张力：简单性与高效性不可兼得，通用性与低开销不可兼得。因此，我们的内存管理子系统采用分层设计策略，将不同阶段、不同粒度的需求分配给不同的分配器处理，每一层只解决自己面对的问题，层与层之间通过清晰的接口协作。

这种分层策略的核心思想是"渐进式能力构建"：底层分配器为上层分配器提供构建材料，上层分配器利用这些材料提供更精细的服务。底层分配器能力有限，但足以支撑上层分配器的运转；上层分配器功能完备，但依赖底层分配器已经就绪。这种依赖关系是单向的、不可逆的，保证了系统在启动过程中能够按确定的顺序逐步建立起完整的内存管理能力，而不会陷入循环依赖的困境。

== 分层架构与职责划分

内存管理子系统由若干个层次构成，从底至上依次为早期内存分配器（Boot）、伙伴分配器（Buddy）、虚拟地址空间管理器、Slab 分配器、内核堆分配器，以及位于最顶层的进程地址空间管理器。每一层都有明确的职责边界和接口契约，上层通过下层提供的接口获取内存资源，但不关心下层如何实现这些接口。这种分层关系并非简单的堆叠，而是一种能力递进的关系：下层提供的是原始的、粗粒度的内存资源，上层在这些资源的基础上构建出更精细、更高效的分配服务。

分层架构的设计哲学源于对系统复杂性的分解。在没有分层的系统中，所有分配请求都经过同一个算法处理，这个算法必须在各种极端情况下都能工作：既要处理 8 字节的请求也要处理 8 MB 的请求，既要应对高频分配也要应对低频分配，还要在碎片化严重的内存状态中继续工作。这样的算法要么过于复杂，要么在某些场景下效率极低。分层架构通过对问题空间进行分类，让每个分配器只关注一个特定的子问题，从而在各自领域内达到最优性能。

#figure(caption: figure-caption("图", "2-1", [内存管理子系统分层架构]))[
  #layer-card("早期内存分配器（Boot）", "在正式分配器就绪之前提供最原始的分配能力，基于线性递增指针实现，只分配不回收", fill: warm-fill)
  #flow-arrow(label: "提供物理页帧")
  #layer-card("伙伴分配器（Buddy）", "管理所有可用物理页帧，以 2 的幂次方为粒度进行分配与回收，解决外部碎片问题", fill: soft-fill)
  #flow-arrow(label: "提供页帧与虚拟区间")
  #layer-card("虚拟地址空间管理器", "管理内核堆虚拟地址区间的分配与释放，协调虚拟地址与物理页帧的映射关系", fill: soft-fill)
  #flow-arrow(label: "提供小对象缓存与大对象分配")
  #layer-card("Slab 分配器 + 内核堆分配器", "Slab 为高频小对象提供对象缓存，内核堆为大对象和页级对齐分配提供服务", fill: stable-fill)
  #flow-arrow(label: "提供进程地址空间管理")
  #layer-card("进程地址空间管理器", "管理用户态进程的虚拟内存区域、缺页处理、写时复制和共享映射", fill: stable-fill)
]

表 2-1 概括了各层分配器的职责边界和关键特征。

#continued-table(
  "2-1",
  [内存管理各层职责],
  (1.5fr, 2fr, 2fr, 1.5fr),
  (
    table.cell(fill: soft-fill)[#text(weight: "bold")[层次]],
    table.cell(fill: soft-fill)[#text(weight: "bold")[核心职责]],
    table.cell(fill: soft-fill)[#text(weight: "bold")[分配粒度]],
    table.cell(fill: soft-fill)[#text(weight: "bold")[回收能力]],
  ),
  (
    [早期内存分配器], [提供启动期最原始的分配能力], [任意字节对齐], [不支持],
    [伙伴分配器], [管理物理页帧的分配与回收], [页帧的 2 的幂次方], [完整支持],
    [虚拟地址空间管理器], [管理内核堆虚拟地址区间], [虚拟地址区间], [完整支持],
    [Slab 分配器], [为高频小对象提供缓存], [对象大小], [完整支持],
    [内核堆分配器], [为大对象提供分配服务], [任意大小], [完整支持],
    [进程地址空间管理器], [管理用户态地址空间], [页和区域], [完整支持],
  ),
  kind: "表",
  continuation-kind: "续表",
  align: (left, left, left, left),
)

分层架构中的每一层都有明确的职责边界和设计目标，这种边界的划分基于两个核心原则：分配粒度的差异性和访问模式的差异性。前者是指不同大小的请求往往有不同的最优算法——小块分配需要最小化碎片，大块分配需要最小化查找开销。后者是指不同场景下的分配和释放频率不同——高频分配需要最小化分配延迟，低频分配可以接受更复杂的初始化开销。

锁顺序是分层架构中必须严格遵守的工程约束。由于上层分配器在执行分配操作时可能需要获取下层分配器的锁，如果锁的获取顺序不一致，就会产生死锁。我们的内存管理子系统规定了从底至上的锁获取顺序：早期内存分配器的初始化锁最先、伙伴分配器的物理内存锁次之、虚拟地址空间管理器的区间锁再次、Slab 分配器的状态锁随后、内核堆分配器的内部锁最后。任何代码路径都必须按照这个顺序获取锁，绝不允许反序获取。此外，在调用可能触发分配的函数之前，必须先释放当前持有的锁，以避免在持锁状态下触发分配而导致的递归锁请求。

=== 分层协作的溢出机制

分层之间的协作通过溢出机制实现。当某一层无法满足当前的分配请求时，它会向下一层请求更大的内存块，然后在自身内部对这个大块进行细分以满足多个较小的请求。这种"批发转零售"的模式使得内存利用率和分配效率达到平衡。

溢出机制的设计需要解决三个核心问题：溢出阈度的确定、大块管理的策略、以及跨层协调的协议。溢出阈度的确定涉及到分层架构中每层的职责边界。一个常见的设计是让 Slab 分配器处理小于一页的分配请求，当请求超过一页时，直接向页分配器请求整页。阈度的选择需要考虑碎片率：如果阈度过低，会导致大量的页内碎片（对象远小于页帧大小）；如果阈度过高，会导致 Slab 缓存过大而浪费内存。理想情况下，阈度应该根据实际的对象大小分布来动态调整。

大块管理的策略决定了当某一层需要向下一层请求内存时，它应该请求多大的块。这个策略直接影响内存的利用率和分配效率。如果每次只请求刚好满足当前请求的内存，会导致频繁的跨层请求，增加同步开销和碎片化风险。如果每次请求过大的块，虽然可以减少跨层请求频率，但可能造成内存浪费。一个常见的策略是让下层一次性提供多个请求量的内存，填充到上层的缓存中，这样可以在利用率和效率之间取得较好的平衡。

跨层协调的协议定义了层与层之间的交互方式。这个协议必须足够简洁以降低实现复杂度，但又必须足够完整以处理各种边界情况。协议中需要包含分配接口（如何请求内存）、释放接口（如何归还内存）、以及元数据管理（如何跟踪哪些内存属于哪个层）。一个设计良好的跨层协议应该让每一层都可以独立演进，而不需要协调其他层的修改。

这种分层设计的另一个重要优势是职责单一性带来的可测试性和可替换性。每一层都可以独立进行单元测试，只需为其注入下层的模拟实现即可。当某一层的算法需要改进时，只需替换该层的实现而不影响其他层。例如，如果我们发现当前的 Slab 分配器算法在特定场景下性能不佳，可以替换为更优的实现，而无需修改页分配器或虚拟内存管理器。这种模块化设计大大降低了系统演进的成本，使得我们可以在不影响其他组件的情况下持续优化内存管理的各个方面。

== 早期内存分配器

在内核启动的最早期，系统面临一个鸡与蛋的问题：要构建一个功能完备的分配器，首先需要分配内存来建立分配器自身的内部数据结构；而要分配内存，又必须先有一个可以工作的分配器。早期内存分配器正是为了打破这个循环而存在的。它的设计哲学是"够用就好"——不追求高效，不追求通用，甚至不追求回收，只求在启动最早期提供一种可靠的分配手段，使得后续的分配器可以借助它来构建自身。

之所以需要单独设计一个启动分配器，是因为启动阶段与运行阶段面临着截然不同的约束条件。系统刚获得控制权时，处理器可能处于实模式或最小保护模式，虚拟内存尚未启用，动态内存分配所需的栈和堆尚未建立。在这种情况下，任何依赖动态数据结构的分配算法都无法工作。更重要的是，启动分配器必须在最短时间内就绪，因为后续的固件解析、设备初始化和内核堆建立都需要内存支持。启动分配器不能等待复杂的初始化过程，它必须立即响应分配请求。

启动分配器面临的核心问题是：在没有任何分配器的情况下，如何分配内存来建立分配器本身？这是一个典型的"自举"问题，解法是放弃通用性，换取确定性。启动分配器不使用通用的数据结构（如链表或树）来管理空闲内存，而是使用预先分配好的固定缓冲区或简单的线性扫描算法。这些方法在空间效率上远不如现代分配器，但足够简单、足够可靠，能够在任何环境下工作。这个权衡体现了系统设计中的务实原则：在某些约束条件下，可行性比效率更重要。

=== 线性分配器

早期内存分配器的工作原理极其简单：维护一个线性递增的分配指针，每次分配请求到来时，将指针向前移动请求的字节数，然后返回移动前的指针值作为分配结果。这种策略被称为"线性 bump 分配"，优点是分配操作的时间复杂度为常数级，不需要任何复杂的数据结构，也不需要搜索空闲链表；缺点是只能分配、不能回收，分配过的内存无法被重复利用。

线性分配器之所以成为启动阶段的首选，正是因为它放弃了释放能力，换来的是极度简单的状态管理——只需维护一个指针和一个边界即可。这种简单性带来了极致的可靠性：启动阶段出现的任何异常都可能导致系统无法继续运行，而复杂的分配算法可能因边界条件的疏忽而产生错误。线性分配器的代码路径是线性的，没有分支和循环，每一个操作都是确定性的，使得它可以在最恶劣的环境下正常工作。

在启动阶段，内存一旦分配就不会回收，直到运行期分配器接管。因此，线性分配器的这个缺陷在启动场景下并非问题，反而简化了实现。启动阶段分配的内存在整个启动过程中持续使用：从系统初始化到内核堆建立，从设备枚举到文件系统挂载，这段内存的生命周期跨越了整个启动过程。在此期间没有内存需要被释放后再重新分配，线性分配器"有去无回"的特性不会造成任何问题。

#pseudo-sample("2-1", [早期内存分配器的核心数据结构], kind: "代码")[
  ```c
  struct BootAllocator {
      size_t  heap_start;    // 分配池起始地址
      size_t  heap_end;      // 分配池结束地址
      size_t  cursor;        // 当前分配指针
  };

  void* boot_alloc(BootAllocator* alloc, size_t size, size_t align) {
      // 对齐 cursor 到 align 的整数倍
      size_t aligned = (alloc->cursor + align - 1) & ~(align - 1);
      if (aligned + size > alloc->heap_end) {
          return NULL;  // 内存不足
      }
      alloc->cursor = aligned + size;
      return (void*)aligned;
  }
  ```
]

然而，简单的线性分配器也有其局限性。当需要分配不同大小的对象时，线性分配器可能因对齐填充而导致内存浪费。例如，分配一个 100 字节的对象后紧接着分配一个需要 8 字节对齐的 200 字节对象，中间可能产生无法使用的对齐空洞。这种碎片化的浪费在内存紧张的启动阶段可能是致命的。解决方法是采用分段线性分配器：为不同大小类别的对象维护独立的线性分配指针，每个指针只分配其类别范围内的请求。

分段设计的另一个好处是减少碎片。不同大小范围的对象有不同的对齐要求和分配模式，将它们分离到不同的分段可以更好地利用内存空间。小对象通常不需要严格的地址对齐，可以使用更紧凑的分配方式；大对象需要页对齐以便于与后续的页分配器协作。分段设计使得每段都可以针对其主要处理的对象类型进行优化，而不需要在所有类型之间寻找平衡点。

=== 从固件内存图初始化

早期内存分配器的初始化需要从固件提供的内存映射表中提取可用区域。固件（如 UEFI 或 Flattened Device Tree）会提供一个内存区域列表，标明哪些区域是可用的，哪些区域已被固件占用。这个列表是启动分配器唯一的内存信息来源。

固件内存图的解析是启动过程中的关键步骤之一。固件作为硬件和操作系统之间的桥梁，掌握着哪些物理内存区域可用、哪些已被固件或其他固件态软件占用的信息。这些信息对操作系统而言是无价的——它提供了内存布局的全局视图，使操作系统可以安全地进行内存分配而不会覆盖固件正在使用的数据结构。

固件内存图的解析需要处理多种区域类型。保留区域通常包含固件代码和数据，操作系统不能使用；已占用区域可能包含设备映射或固件服务的临时数据，需要谨慎处理；可用区域是操作系统可以直接使用的内存。在解析过程中，操作系统必须准确识别每种区域类型的边界和使用目的，将可用区域纳入启动分配器的管理范围，同时跳过所有不可用区域。

需要特别注意的是，启动分配器的初始化必须跳过内核镜像所在的区域。如果启动分配器覆盖了正在执行的代码，数据损坏会导致系统崩溃。同样，任何固件保留的区域、已映射设备的区域都必须排除在可用范围之外。这些约束使得内存图解析必须精确判断每个区域的性质和用途，不能有任何疏漏。

早期内存分配器从启动上下文提供的可用物理内存段中选择一段作为分配池。启动上下文中的 `StartAllocatorOps` 结构包含了分配器所需的所有回调函数：`kernel_heap_region` 用于获取内核堆的物理内存范围，`map_kernel_heap_range` 用于在页表中建立虚拟地址到物理页帧的映射，`unmap_kernel_heap_range` 用于解除映射，`init_kernel_page_table` 用于安装内核堆所需的架构页表状态。这些回调函数由架构层在构造启动上下文时注入，通用层通过函数指针调用它们，而不直接依赖任何架构特定的页表操作代码。

早期内存分配器的设计中有一个关键的工程决策：只分配、不回收。这并非疏忽，而是有意为之。启动早期，内核分配的内存都是长期存活的——伙伴分配器的内部数据结构、内核页表、设备驱动对象等，一旦分配就不会被释放。因此，早期内存分配器不需要回收能力，这极大地简化了实现。当伙伴分配器初始化完成后，早期内存分配器将剩余的未分配内存移交给伙伴分配器，自身正式退役。这种"一次性使用"的设计使得代码极其简洁，也避免了在启动早期引入复杂的内存回收逻辑。

== 伙伴分配器

当系统从启动阶段进入运行阶段，启动分配器需要被功能更完善的页分配器取代。伙伴系统（Buddy System）分配器是页分配器最经典的设计，以简单、高效著称，特别适合管理大块连续内存的分配与回收。它是早期内存分配器的继任者：在早期内存分配器退役之后，伙伴分配器接管所有物理页帧的管理权，成为运行期物理内存分配的唯一入口。

=== 伙伴系统的基本原理

伙伴系统的核心思想是将内存按 2 的幂次方组织成不同阶数的空闲块。每个阶数对应一种块大小：阶数 0 对应一个页帧，阶数 1 对应两个连续页帧，阶数 2 对应四个连续页帧，以此类推。系统为每个阶数维护一个空闲链表。分配时，从满足请求大小的最小阶数的空闲链表中取出一个块；如果该阶数的空闲链表为空，则从更高阶数的空闲链表中取出一个块并将其分裂为两个"伙伴"块——一个用于满足请求，另一个插入低一阶的空闲链表。回收时，如果被释放的块的伙伴也是空闲的，则将两者合并为更高一阶的空闲块，这个过程递归进行，直到伙伴不在空闲状态或达到最高阶数为止。

伙伴系统的"伙伴"概念至关重要。两个块成为伙伴的条件是：大小相同、来自同一个父块的分裂、且地址连续。一个块及其伙伴的地址关系具有确定的数学规律：若块的大小为 2^k，起始地址为 P，则其伙伴的起始地址为 P xor (1 << k)。这个规律使得伙伴查找和合并操作可以在常数时间内完成，无需遍历任何数据结构。这个简洁的数学关系正是伙伴系统高效性的根源。

#figure(caption: figure-caption("图", "2-2", [伙伴系统分裂与合并过程]))[
  #flow-node([空闲块 16 页], fill: soft-fill)
  #flow-arrow()
  #flow-node([请求分配 4 页：分裂为两个 8 页块], fill: warm-fill)
  #flow-arrow()
  #flow-node([继续分裂：4 页块满足请求，4 页块保留], fill: soft-fill)
  #flow-arrow()
  #flow-node([释放 4 页块：检查伙伴是否空闲], fill: soft-fill)
  #flow-arrow()
  #flow-node([伙伴空闲：合并为 8 页块], fill: handoff-fill)
  #flow-arrow()
  #flow-node([继续检查：合并为 16 页块], fill: warm-fill)
]

伙伴系统的一个优雅特性是合并过程的高度自动化。当一个块被释放时，分配器自动检查其伙伴是否也空闲；如果是，则将两者合并为一个更大的块。合并后的块又会触发新一轮的伙伴检查，形成向上合并的连锁反应。这个机制确保了内存碎片能够被及时回收：一个 4 KiB 的块被释放时，若其伙伴也空闲，两者会立即合并为 8 KiB 的块；若这个 8 KiB 块的伙伴同样空闲，它们会继续合并为 16 KiB 的块，以此类推。这种自动合并特性大大减少了外部碎片的累积。

#pseudo-sample("2-2", [伙伴分配器的核心数据结构], kind: "代码")[
  ```c
  #define MAX_ORDER  11  // 最高阶数，支持到 2^11 = 2048 个页帧

  struct BuddyAllocator {
      size_t  page_size;                // 单个页帧的字节大小
      size_t  total_pages;              // 管理的总页帧数
      struct FreeList free_lists[MAX_ORDER + 1];  // 每个阶数的空闲链表
      struct Page*    page_array;       // 页帧描述符数组
      uint8_t*        order_map;        // 每个页帧所属的空闲块阶数
  };
  ```
]

=== 分配算法

伙伴系统的分配算法遵循"向上分裂"策略。当请求一个大小为 order 阶的块时，分配器首先检查 free_lists[order] 是否有空闲块。如有，直接分配并返回；如无，检查 free_lists[order+1]，如此递归向上直到找到非空链表或超出最大阶数。如果找到非空链表，取出第一个块，分裂为两个伙伴，其中一个满足请求，另一个挂入 lower_order 的空闲链表。

向上分裂策略的选择有其深刻的合理性。当需要分配一个 4 KiB 的块但没有直接的空闲块时，分配器不是放弃或报错，而是向上寻找一个更大的块并分裂它。这确保了只要有足够大的空闲区域，分配请求最终一定能被满足。一个 4 MiB 的空闲块可以分裂成两个 2 MiB 的块，一个 2 MiB 的块可以分裂成两个 1 MiB 的块，如此递归，直到产生满足请求的 4 KiB 块。整个分裂过程是确定性的，不依赖于内存的分配历史。

分裂操作完成后，一半的内存被分配给请求者，另一半成为较小一级的空闲块。这个新产生的空闲块被加入到相应等级的空闲链表中，以后可能用于满足较小的分配请求。这种设计使得伙伴系统能够自适应地处理各种大小的分配请求：如果大部分请求是 4 KiB 的，那么空闲链表中会有大量的小块；如果大部分请求是 1 MiB 的，那么空闲链表中会有大量的中等大小的块。

=== 释放与合并算法

伙伴系统的释放算法是分配算法的逆过程。当释放一个阶数为 order 的块时，首先更新 order_map，然后将块挂入 free_lists[order]。关键步骤是检查伙伴块是否空闲，如果是则合并为更大的块，递归向上直到无法合并为止。

伙伴查找利用地址规律进行。块起始地址 P，大小为 2^order，伙伴起始地址为 P xor (1 << order)。这个规律使得伙伴查找不需要任何复杂的数据结构，只需要知道当前块的地址和阶数即可。在数学上，这个关系来自于二进制表示的位翻转特性：如果一个数的二进制表示是 xxx1000（其中 xxx 表示高位），那么它的伙伴（同一级别的另一个块）的地址就是将第 k 位（0-indexed）翻转后的值。

合并操作的正确性依赖于空闲链表的状态。检查伙伴是否空闲时，必须持有对应阶数空闲链表的锁，以确保在检查和移除之间的原子性。如果多个 CPU 同时释放相邻的伙伴块，合并操作需要正确处理并发情况。本系统通过在关键步骤中使用自旋锁来保证这种原子性：获取锁后检查伙伴状态，如果空闲则移除并合并，然后释放锁。这个过程虽然涉及到锁的开销，但对于内存分配这种相对低频的操作来说，这个开销是可以接受的。

=== 伙伴系统的局限性

伙伴系统分配器虽然简单高效，但也存在固有的局限性。最显著的问题是内部碎片（Internal Fragmentation）：当请求的大小不是 2 的幂次时，分配器会分配一个不小于请求大小的 2 的幂次块，导致部分内存无法使用。例如，请求 6 KiB 的内存会分配 8 KiB 的块，浪费 2 KiB。

内部碎片的根本原因是分配粒度与请求大小的不匹配。在伙伴系统中，所有块的大小都是 2 的幂次，但应用程序的内存请求往往不是这个粒度的倍数。一个请求 1000 字节的对象会被分配 1024 字节的空间，其中有 24 字节是浪费的。虽然这个浪费比例看起来不大，但累积起来可能造成显著的内存损失。

另一个局限性是外部碎片（External Fragmentation）在长期运行后的累积。虽然伙伴系统的合并机制能够有效减少外部碎片，但经过大量的分配和释放循环后，空闲块可能分散在不同的阶数中，导致无法满足大块请求而整体内存仍然充足的情况。这需要定期的内存整理或更高级的分配策略来缓解。

为了解决这些问题，实际系统通常在伙伴系统之上叠加其他分配策略，如 Slab 分配器。Slab 分配器专门处理小于一页的小对象分配，避免了伙伴系统在小对象上的碎片问题。两者结合，使得内存管理系统既能高效处理大块分配，又能灵活处理小对象的高频分配。

== Slab 分配器

Slab 分配器是解决小对象分配问题的关键机制。当系统需要频繁分配和释放小块内存时（如内核数据结构、文件系统的 inode 缓存等），直接使用页分配器会导致严重的碎片化和分配开销。Slab 分配器的设计思想是为每种对象类型维护独立的内存缓存，每个缓存由多个"Slab"组成，每个 Slab 是完整的物理页，按照对象大小被等分为多个槽位。

Slab 分配器诞生的背景是操作系统内核中存在大量固定大小对象的反复分配和释放，包括进程描述符、文件描述符、inode、dentry、缓冲区头、网络包描述符等。如果每次分配这些对象都直接从伙伴系统请求一个新的页帧，浪费会非常严重：一个进程描述符可能只需要几百字节，但一个页帧是 4 KiB；一个缓冲区头可能只需要几十字节，却同样占用整个页帧。更糟糕的是，这种分配方式会产生大量内部碎片，因为每个页帧中只有一个对象被使用，其余空间都被浪费了。

Slab 分配器的核心优势在于分配和释放的极高效率。在缓存不空的情况下，分配操作只是从链表中取出一个空闲槽位，时间复杂度为 O(1)；释放操作只是将槽位归还到链表，时间复杂度同样为 O(1)。没有伙伴系统的分裂合并开销，没有空闲列表的查找搜索，只有纯粹的对象复用。这种效率对高频分配场景至关重要：试想一个高并发的文件服务器，每秒钟可能需要创建和销毁数千个文件描述符，如果每次分配都需要遍历空闲链表或进行页表操作，系统性能将受到严重影响。

#figure(caption: figure-caption("图", "2-3", [Slab 分配器的内存布局]))[
  #flow-node([Slab：完整的物理页（4 KiB）], fill: warm-fill)
  #flow-arrow()
  #flow-node([对象槽位：等分为 N 个对象空间], fill: soft-fill)
  #flow-arrow()
  #flow-node([空闲链表：指向可用对象], fill: handoff-fill)
]

=== Slab 的结构设计

一个 Slab 的内存布局包含三个区域：元数据区存储 Slab 的管理信息（如空闲对象链表、空闲对象计数等）；对象区存储实际分配的对象；可能存在的着色区用于缓存行对齐优化。元数据区通常很小，只占用几个字节到几十个字节，它们被组织在 Slab 的开头或结尾。对象区占据 Slab 的主体，被等分为 N 个大小相等的槽位，每个槽位可以存储一个对象实例。

slabs_partial、slabs_full、slabs_free 三个链表分别管理不同状态的 Slab。这种三级分类的设计使得分配算法可以高效地找到最优目标 Slab。slabs_full 中的 Slab 不会再收到新的分配请求，因为所有对象都已使用；slabs_free 中的 Slab 可以直接分配任何空闲对象；slabs_partial 中的 Slab 既有空闲对象又有已占用对象，分配时优先使用。这种优先使用原则确保了空闲的 Slab 能够保留更多的空闲空间，以应对突发的分配高峰。

=== 对象分配与释放

Slab 分配器的分配算法首先检查 slabs_partial 链表是否非空，如有则从该 Slab 中分配一个对象。分配操作只需要从 Slab 的 free_objects 链表中取出一个对象即可。这个链表的组织非常巧妙：每个空闲对象槽位的开头几个字节被用作指向下一个空闲对象的指针，形成一个链表。当对象被分配时，这个指针被返回给调用者；当对象被释放时，指针被更新为指向原来的 free_objects 链表头。这种原地利用对象槽位的做法避免了在 Slab 中额外分配元数据空间。

如果 slabs_partial 为空，检查 slabs_free 链表；如有则移动一个 Slab 到 slabs_partial 并从中分配。如果两者都为空，向伙伴系统申请新的页帧，构造新的 Slab 并加入 slabs_free，然后重新执行分配流程。这个递归调用看起来可能有性能问题，但实际上只会发生一次：第一个条件检查 slabs_partial 会失败，第二个条件检查 slabs_free 会成功（或者失败并创建新的 Slab），然后递归调用会直接命中 slabs_partial 并完成分配。

释放操作是分配操作的逆过程。当释放一个对象时，根据对象的地址计算出所属的 Slab，然后检查 Slab 的 inuse 计数。如果从满变为不满，需要将 Slab 从 slabs_full 移动到 slabs_partial。最后将对象添加到 Slab 的空闲链表头部。释放操作的效率与分配操作一样，都是常数时间复杂度。

=== Slab 着色与缓存行对齐

Slab 着色（Coloring）是一个优化技术，用于减少不同 Slab 之间对象的缓存行冲突。当多个 Slab 的同一偏移位置（通常是对象起始位置）对齐到相同的缓存行时，频繁访问这些对象的代码可能导致缓存行抖动（Thrashing）。现代处理器的缓存系统使用缓存行作为基本单元，通常是 64 字节。当两个对象位于同一个缓存行时，访问其中一个可能导致另一个被移出缓存，这对于高频访问的数据结构来说是性能损失。

Slab 着色通过在 Slab 开头添加一个偏移来错开不同 Slab 中对象的缓存行位置，从而减少冲突。这个偏移被称为"颜色"，因为它最初被用于区分不同的 Slab（类似于给不同的 Slab 涂上不同的颜色以便区分）。不同的 Slab 使用不同的着色偏移，使得它们的对象在物理上错开分布。

着色偏移的范围取决于对象大小和一个 Slab 能容纳的对象数。每个 Slab 的对象数是 `floor((PAGE_SIZE - sizeof(Slab)) / object_size)`，着色偏移 range 是 `PAGE_SIZE - total * object_size`。通过为不同的 Slab 分配不同的着色偏移，可以让不同 Slab 的对象在缓存中错开分布。例如，如果一个 Slab 的第一个对象位于缓存行 0，那么着色 1 的 Slab 的第一个对象位于缓存行 1，以此类推。

着色技术是一个可选的优化，不是 Slab 分配器的核心功能。它的效果取决于工作负载的特性：对于访问模式随机分布的对象，着色的效果不明显；但对于访问模式高度局部化的对象（如哈希表的桶数组），着色的效果可能非常显著。在实际实现中，需要根据性能测试的结果来决定是否启用着色优化。

== 内核堆管理器

内核堆管理器（Kernel Heap Manager）是内存管理体系中处理大块内存分配的核心组件。当 Slab 分配器无法满足分配请求（通常是请求大小超过几 KiB）时，请求会向上传递给内核堆管理器。它与页分配器直接交互，获取大块内存，并通过虚拟内存管理实现复杂的映射能力。

内核堆管理器的设计目标与下层分配器不同。下层分配器（伙伴系统、Slab）主要关注物理内存的高效利用和碎片控制；内核堆管理器则需要提供更丰富的语义，如内存映射、延迟回收、按需增长等。这些高级特性使得内核堆管理器成为内核动态内存分配的主要接口，应用程序和内核组件通过这个接口来进行大多数的内存分配操作。

在设计内核堆管理器时，我们面临一个与用户态堆管理器不同的问题：内核地址空间是有限的。用户态进程拥有巨大的虚拟地址空间，而内核的地址空间受到架构限制（通常只有 48 位或 57 位有效地址），且需要同时为所有进程服务。这个约束使得内核堆管理器不能像用户态堆管理器那样假设地址空间无限，必须谨慎管理内核虚拟地址空间的消耗。

=== 虚拟地址空间布局

内核堆管理器需要在内核地址空间中预留一段连续的虚拟地址范围用于堆管理。这段地址范围通常位于内核镜像和直接映射区域之间，大小取决于系统内存规模和预期负载。内核堆管理器在这段范围内管理多个独立的堆区域，每个区域服务于不同的分配模式。

不同的堆区域服务于不同的分配模式，这种分离设计可以提高内存管理的效率。通用分配区域用于常规的内核内存分配，大小适中，分配策略侧重于减少碎片；大块分配区域专门服务于超过数个页的分配请求，预留较大的连续虚拟空间，采用不同的分配策略以适应大块分配的特点；持久分配区域用于需要长期占用、不会释放的内存分配，避免与通用区域竞争，减少碎片管理的复杂度。

=== 大块分配与延迟回收

内核堆管理器处理大块分配请求时，不会立即从伙伴系统获取页帧。它维护一个内存池，当请求到达时，首先检查内存池是否有足够的空闲内存；如有，直接从内存池分配；如无，向下层的虚拟内存子系统请求新的虚拟地址范围，并按需映射物理页。

延迟回收（Lazy Reclaim）是内核堆管理器的重要特性。当应用程序释放大块内存时，内核堆管理器不会立即将物理页归还给伙伴系统，而是保留这些页并标记为可回收，等待未来需要时再使用。这种策略减少了伙伴系统的分配和释放开销：在频繁分配和释放大块内存的场景中（如文件系统的缓存），如果每次释放都立即归还给伙伴系统，下次分配时又需要重新请求，这个循环会产生大量的碎片化和合并操作。延迟回收通过保留已释放的内存来避免这种开销。

延迟回收的另一个优势是它可以减少分配延迟。当一个分配请求到达时，如果内存池中有可用的空闲块，可以立即满足请求而不需要等待伙伴系统的分配操作。对于那些对延迟敏感的操作（如中断处理程序或实时任务），这种即时分配的能力是非常重要的。

延迟回收的触发条件通常是内存池大小超过某个阈值或空闲时间达到预设值。回收策略可以是激进或保守的：激进的回收策略在满足条件时立即释放尽可能多的内存，适用于内存资源紧张的场景；保守的回收策略保留更多的缓存以应对未来的分配请求，适用于分配模式不可预测的场景。实际选择哪种策略需要根据工作负载的特性来决定。

=== 内存映射与增长

内核堆管理器的一个重要特性是支持动态增长。当现有虚拟地址空间不足以满足分配请求时，堆管理器可以向虚拟内存子系统申请扩展堆区域。这种按需增长的模式使得堆区域可以开始于较小的地址空间，并根据实际负载逐渐扩展。

内存映射的过程涉及虚拟地址空间的管理和新页帧的分配。当请求分配一块内存时，堆管理器首先计算需要多少个页帧，然后检查空闲虚拟地址范围是否足够。如果空闲范围不足够，向虚拟内存子系统申请新的虚拟地址范围，这个申请过程可能涉及页表的创建和更新。获得虚拟地址范围后，逐页分配物理页并建立映射。

增长操作的失败处理至关重要。如果虚拟地址范围申请成功但物理映射失败，必须回滚虚拟地址范围的分配，避免留下空洞的虚拟地址空间。这种回滚必须与增长操作在同一个临界区中完成，以避免并发问题导致的状态不一致。

== 虚拟内存管理

虚拟内存管理是进程视角的内存管理，它维护每个进程的虚拟地址空间布局，将虚拟地址映射到物理页。虚拟内存管理器（VmSpace）是进程地址空间的抽象，它提供地址空间的创建、映射、修改和销毁等功能。

虚拟内存管理的核心概念是页表（Page Table）。页表是一个树状结构，用于将虚拟地址转换为物理地址。现代处理器使用多级页表（如 x86-64 的四级页表、ARM 的四级页表等），以减少单个页表的大小并支持稀疏的地址空间映射。若没有多级页表，一个 64 位地址空间需要 2^64 个条目才能完全映射，这显然不可行。多级页表通过分级索引的方式，将连续的虚拟地址空间分割成多个层级，每个层级只需为实际使用的部分分配页表页。

虚拟内存管理器负责建立和维护这些页表，处理页错误（Page Fault），实现内存保护和共享等高级特性。当应用程序访问一个尚未映射的虚拟地址时，处理器会产生页错误异常，虚拟内存管理器需要捕获这个异常，判断是合法的需求加载还是非法的越界访问，然后进行相应的处理。

#figure(caption: figure-caption("图", "2-4", [虚拟地址空间布局]))[
  #flow-node([未使用区域：用户代码可自由映射], fill: soft-fill)
  #flow-arrow()
  #flow-node([堆区域：向上增长，动态分配], fill: handoff-fill)
  #flow-arrow()
  #flow-node([内存映射区域（mmap）：匿名或文件映射], fill: soft-fill)
  #flow-arrow()
  #flow-node([栈区域：向下增长，函数调用相关], fill: warm-fill)
  #flow-arrow()
  #flow-node([用户库区域：共享库映射], fill: soft-fill)
  #flow-arrow()
  #flow-node([内核区域：不可访问，用户态访问触发异常], fill: stable-fill)
]

=== 地址空间的创建与销毁

每个进程拥有独立的地址空间，这个地址空间在进程创建时初始化，在进程销毁时回收。地址空间的创建涉及页表的初始化、默认映射的建立、以及相关元数据结构的分配。页表是地址空间的核心数据结构，它由一系列页表页组成，这些页表页本身也需要占用物理内存。在进程创建时，需要分配根页表页并初始化为零，然后建立初始的地址空间布局。

地址空间的销毁涉及所有映射的清除和资源的回收。这个过程必须遍历地址空间中的所有映射区域，逐个解除物理页的映射并释放页帧。忽略任何一步都可能导致内存泄漏或资源泄漏。最后，页表页本身也需要被递归释放，这个过程从根页表开始，逐层向下释放所有已分配的页表页。

地址空间创建后，并不是所有的虚拟地址都立即映射到物理页。实际上，大多数虚拟地址在首次访问之前都是未映射状态，这种按需映射的策略称为需求分页（Demand Paging）。需求分页的好处是可以延迟物理内存的分配，减少启动时的内存消耗。对于那些从未被访问的地址（如程序加载了大量库但实际只使用其中一小部分函数），这种策略可以显著节省物理内存。

=== 内存映射与页错误处理

内存映射是虚拟内存管理的核心操作。当进程请求映射一块内存时（如 mmap 系统调用），虚拟内存管理器在地址空间中分配一个虚拟区域，建立从虚拟地址到物理页的映射，并更新页表。内存映射可以用于多种目的：匿名映射用于分配私有内存，文件映射用于将文件内容映射到地址空间，设备映射用于直接访问设备内存。

页错误处理是虚拟内存管理的另一个核心操作。当进程访问一个尚未映射物理页的虚拟地址时，处理器产生页错误异常。虚拟内存管理器需要处理这个异常，判断错误原因（是正常的需求加载还是非法访问），如果是正常的需求加载则分配物理页并建立映射，如果是非法访问则向进程发送信号或终止进程。

页错误处理需要区分不同的错误类型。合法的页错误（如需求加载、Copy-on-Write）是正常的工作流程，处理后进程可以继续运行；非法的页错误（如越界访问、权限违反）需要向进程发送 SIGSEGV 信号或执行其他错误处理。这个区分依赖于错误发生时的地址、访问类型、以及虚拟内存区域的属性。

页错误处理器的性能直接影响系统的整体性能。每次页错误都需要从用户态切换到内核态，执行错误处理代码，然后返回用户态。这个上下文切换的开销在高频页错误场景中可能是显著的。因此，优化页错误处理路径是虚拟内存子系统性能优化的重要方向。一些常见的优化包括：使用大页（Huge Page）减少页错误频率，在处理页错误时预先读入可能需要的相邻页，以及使用批处理合并多个相邻的页错误。

=== Copy-on-Write 机制

Copy-on-Write（COW，写时复制）是虚拟内存管理的重要优化技术。当多个进程共享同一块物理内存时，可以将页表条目设置为只读状态，而非立即复制物理页。只有当某个进程尝试写入时，才会触发页错误，操作系统在错误处理中复制物理页并建立新的映射。这个机制显著减少了 fork 时的内存复制开销。

在传统的 fork 实现中，父进程的整个地址空间需要被复制到子进程，时间复杂度与地址空间大小成正比。对于运行着大型应用程序的进程，其地址空间可能包含数十个 MiB 的数据，全部复制一遍会消耗大量时间和内存。Copy-on-Write 的核心思想是推迟复制操作：fork 时，父子进程共享同一个物理页，两个进程的页表条目都指向这个物理页但都设置为只读；只有当其中一个进程尝试写入时，才触发复制和映射更新。

Copy-on-Write 的关键在于跟踪共享页面的引用计数。当 fork 时，原进程的物理页面不会被复制，而是同时映射到父进程和子进程的地址空间。两个进程的页表条目都设置为只读，并且页面标记为共享。当任一进程尝试写入时，触发页错误，处理程序分配新页面、复制内容、更新该进程的页表条目并标记为可写。如果页面引用计数降到 1，则恢复独占状态。

Copy-on-Write 的一个重要应用是在写时复制fork（COW fork）机制中。在 COW fork 的实现中，fork 系统调用的返回时间不再与父进程的地址空间大小成正比，而是几乎恒定的——只需要复制少数必要的内核数据结构（如进程描述符、页表等），用户态地址空间的内容并不立即复制。只有当父进程或子进程实际写入某个页面时，该页面才会被复制。这种机制大大加快了 fork 的速度，使得频繁创建子进程的应用（如 Web 服务器、数据库等）能够更高效地运行。

== 垃圾回收机制

随着系统运行时间的增长，内核内存中可能出现不再使用但未能及时释放的对象。这些"泄漏"的内存在长期运行后可能累积并导致内存压力。垃圾回收（Garbage Collection，GC）机制是解决这一问题的方案，它自动识别和回收不再使用的内存对象。

传统的内存管理依赖于程序员显式分配和释放内存。这种方式效率高，但也容易引入两类错误：一是内存泄漏（Memory Leak），即分配了内存但忘记释放，导致内存逐渐耗尽；二是野指针（Dangling Pointer），即释放了内存但继续使用，导致潜在的崩溃或安全问题。垃圾回收机制通过自动追踪对象的使用情况来确定哪些对象可以被安全回收，从而消除内存泄漏的问题。

本系统的垃圾回收采用基于追踪（Tracing）的算法，通过可达性分析（Reachability Analysis）确定哪些对象仍然被引用。从根集合（如全局变量、栈变量）出发，遍历所有可达对象，未能遍历到的对象即为不可达，可以安全回收。这种算法的优势是无需程序员显式释放内存，减少了泄漏的可能性；劣势是回收过程本身有时间和空间开销，以及无法处理循环引用（除非使用额外的机制如弱引用）。

=== 可达性分析与根集合

垃圾回收的核心是可达性分析。一个对象是可达的，当且仅当存在一条从根集合到该对象的引用路径。根集合包括静态分配的对象（如全局变量）、活跃线程的栈帧中的局部变量、以及其他已知活跃的数据结构。任何不在根集合可达范围内的对象都是垃圾，可以被回收。

可达性分析的理论基础是图遍历。在内存分配的视角下，所有对象和引用关系可以构成一个有向图，对象是图的节点，引用是图的有向边。根集合是这个图的起始节点集合。从根集合出发，通过图的边可以到达所有被使用的对象；那些无法到达的节点就是孤立的子图，它们不再被任何代码引用，因此是垃圾。

根集合的确定依赖于语言和运行时的具体实现。在内核环境中，根集合通常包括全局数据段中的变量、当前 CPU 的栈帧、以及内核分配的各种缓存和表。全局数据段的变量因为其生命周期与程序相同，总是可达的。栈帧中的变量只在其所属函数调用期间可达，这是通过追踪当前活跃的调用栈来确定的。内核缓存和表需要被显式注册为根或通过特殊机制来追踪。

=== 标记-清除算法

标记-清除（Mark-and-Sweep）是最基础的垃圾回收算法。它分为两个阶段：标记阶段遍历所有可达对象并标记；清除阶段遍历所有对象，回收未被标记的对象。

标记阶段需要正确处理所有引用类型。指针字段需要被识别并递归扫描；非指针字段（如整数、枚举）需要被忽略。为实现这一区分，垃圾回收器需要知道对象的布局信息，特别是哪些字段是指针。这个信息可以由编译器生成，也可以通过运行时的类型信息获取。在静态类型的语言中，对象的内存布局在编译时已知，可以生成元数据来描述每个字段的偏移和类型。

暂停所有用户线程是标记-清除算法的必要步骤。在扫描过程中，如果允许用户线程运行并修改引用关系，可能导致漏标（遗漏应当保留的对象）或错误标（标记应当回收的对象）。这种"Stop-the-World"的暂停对于实时系统可能是不可接受的，因此更高级的算法（如增量回收或并发回收）被开发出来以减少暂停时间。增量回收将标记过程分成多个小步骤，在每一步之间允许应用程序运行；并发回收在应用程序运行的同时进行标记和清除。这些技术都试图在停顿时间和回收效率之间取得更好的平衡。

=== 分代回收假设

分代回收（Generational GC）是基于"大多数对象生命周期很短"这一经验观察的优化技术。它将堆分为多个代（Generation），新分配的对象位于年轻代，经过多次回收仍然存活的对象被晋升到年老代。由于大多数对象很快就会变成垃圾，只对年轻代进行频繁回收即可显著减少每次回收的工作量。

这个"大多数对象生命周期很短"的现象被称为分代假设，它是许多垃圾回收算法的理论基础。分代假设的合理性可以从程序行为的角度来理解：新分配的对象通常是临时性的——函数内的局部变量、循环中的临时缓冲区等；只有少数核心数据结构需要长期存活，如应用程序的全局配置和缓存池。因此，如果回收算法能够优先关注新分配的对象，就能在较少的遍历中回收更多的垃圾。

分代回收的另一个优势在于它可以利用对象存活的局部性。新分配的对象通常与最近创建的其他对象在物理上相邻（因为分配算法通常从同一区域连续分配）。当这些对象一起成为垃圾时，扫描年轻代可以在较少的内存访问中完成回收，从而提高缓存命中率。

以下代码展示了分代回收的核心设计：

#pseudo-sample("2-3", [分代回收设计], kind: "代码")[
  ```c
  struct Generation {
      size_t size;                 // 代的总大小
      size_t used;                 // 已使用大小
      void* start;                 // 代起始地址
      unsigned survive_threshold;  // 晋升阈值（经历多少次回收）
      unsigned collect_count;      // 该代的回收次数
  };

  struct GenerationalHeap {
      struct Generation young;    // 年轻代
      struct Generation old;      // 年老代
      size_t promotion_age;       // 晋升年龄
      size_t young_ratio;         // 年轻代占堆的比例
  };

  void minor_collection(struct GenerationalHeap* heap) {
      // 只扫描年轻代
      scan_generation(&heap->young, heap->young.used);

      // 检查存活对象是否达到晋升条件
      struct ObjectHeader* obj = heap->young.start;
      while (obj < heap->young.start + heap->young.used) {
          if (object_survived_enough(obj, heap->promotion_age)) {
              promote_object(heap, obj);
          }
          obj = next_object(obj);
      }

      // 重置年轻代使用指针
      heap->young.used = 0;
  }
  ```
]

晋升阈值的选择是一个重要的调参问题。阈值过低会导致本应短寿的对象过早晋升到年老代，增加年老代的回收压力；阈值过高则会导致长寿命对象在年轻代中被反复扫描，抵消分代带来的收益。实践中通常选择 3 到 5 次年轻代回收作为默认晋升条件，更精细的实现还可以根据运行时的对象存活率动态调整这一阈值。

== 进程地址空间管理器

进程地址空间管理器位于内存管理子系统的最顶层，它管理用户态进程的虚拟内存区域（VMA）、缺页处理、写时复制和共享映射。进程地址空间管理器是内存管理子系统与进程管理子系统之间的桥梁，它将底层的物理页帧和虚拟地址空间抽象为进程可以使用的内存资源。

=== VMA 集合与区域管理

VMA（Virtual Memory Area）是进程地址空间中一段连续的虚拟地址范围，每个 VMA 有自己的属性（可读、可写、可执行）、后备存储类型（匿名、文件、设备）和映射状态。进程地址空间管理器维护一个 VMA 集合，按起始地址排序，支持插入、删除、查找和合并操作。

VMA 集合使用有序映射（`BTreeMap`）组织，以起始地址为键。有序映射支持在 O(log n) 时间内根据地址查找对应的 VMA，也支持范围遍历以找到与给定地址范围重叠的所有 VMA。这种数据结构的选择源于 VMA 操作的访问模式：缺页处理需要根据虚拟地址查找对应的 VMA，mmap 需要在地址空间中找到足够大的空闲区域，munmap 需要找到与给定范围重叠的所有 VMA 并进行分割或删除。之所以选择 `BTreeMap` 而非侵入式红黑树或区间树，是因为 Rust 标准库的 `BTreeMap` 已经提供了所需的全部操作语义，且无需手动管理节点内存，在当前的 VMA 规模下性能足够。如果未来需要支持大量 VMA 的高频范围查询，可以在不改变外部接口的前提下替换为真正的区间树。

VMA 的合并是减少碎片的重要机制。当两个相邻的 VMA 具有相同的属性和后备存储时，它们可以被合并为一个更大的 VMA。合并操作减少了 VMA 集合中的条目数量，降低了查找和遍历的开销，也减少了页表中的映射数量。例如，当进程连续调用两次 mmap 分配相邻的匿名内存时，如果两次分配的属性相同，VMA 管理器会自动将它们合并为一个 VMA，而不是维护两个独立的 VMA。

=== 缺页处理的分类策略

缺页处理是进程地址空间管理器最核心的操作之一。当进程访问一个尚未映射的虚拟地址时，处理器产生缺页异常，内核进入缺页处理程序。缺页处理程序需要根据缺页的地址、访问类型和 VMA 的属性，判断缺页的类型并执行相应的处理。

缺页处理程序将缺页分为五类。匿名映射缺页发生在进程首次访问匿名映射的虚拟地址时，处理方式是分配一个零页并建立映射。文件映射缺页发生在进程首次访问文件映射的虚拟地址时，处理方式是从文件中读取对应的数据页并建立映射。栈扩展缺页发生在进程访问栈区域中尚未映射的地址时，处理方式是扩展栈 VMA 并分配零页。写时复制缺页发生在进程写入共享页时，处理方式是复制物理页并更新映射。权限违反缺页发生在进程访问不具有相应权限的虚拟地址时，处理方式是向进程投递 SIGSEGV 信号。

这种分类处理的设计避免了误判导致的内核崩溃或安全漏洞。如果缺页处理程序不能正确区分匿名映射缺页和权限违反缺页，可能会在进程越界访问时分配新的物理页而不是投递信号，导致进程可以访问不应该访问的内存。如果缺页处理程序不能正确处理写时复制缺页，可能会在进程写入共享页时直接修改共享页的内容，破坏其他进程的数据。

=== 异常表与安全边界

异常表（Exception Table）是内核安全访问用户态内存的关键机制。内核在执行 `copy_from_user` 和 `copy_to_user` 等操作时，需要访问用户态的内存地址。这些地址是不可信的，用户程序可能传入一个无效的地址，试图触发内核的缺页处理从而导致未定义行为。异常表通过在内核镜像中注册"安全恢复点"来解决这个问题：当内核访问用户态地址触发异常时，异常处理程序在异常表中查找触发异常的指令地址，如果找到匹配项，则跳转到对应的恢复点，而不是触发内核缺页处理。

异常表机制将内核对用户态内存的安全访问从"先检查后访问"的模式转变为"乐观执行、异常修复"的模式。传统的"先检查后访问"模式需要在每次访问前验证用户态地址的有效性，这个验证本身就需要访问页表，开销较大。异常表机制省略了验证步骤，直接执行访问操作，如果访问成功则路径最短，如果访问失败则通过异常表恢复。这种"乐观执行"的策略在正常路径上消除了安全检查的开销，在异常路径上提供了可靠的恢复手段。

=== 架构注入契约

进程地址空间管理器通过四个操作表将架构相关的细节封装在架构层内部，通用层通过函数指针调用这些操作，从而实现了平台无关的地址空间管理逻辑。这四个操作表分别是：`PageTableOps`（页表操作）、`TlbOps`（TLB 操作）、`AsidOps`（地址空间标识符操作）和 `FaultOps`（缺页分类操作）。

架构注入契约模式使得整个进程地址空间管理可以跨架构复用。四个操作表将所有架构相关的细节封装在架构层内部，通用层通过函数指针调用这些操作，从而实现了平台无关的地址空间管理逻辑。这种封装的意义不仅在于代码复用——更重要的是，它将架构相关的知识集中到了架构层，使得通用层的开发者不需要理解每种处理器的页表格式和异常模型，而架构层的开发者只需要实现操作表中的函数，不需要理解上层的 VMA 管理和缺页处理逻辑。这种知识的隔离降低了开发的认知负担，也减少了跨层修改引入缺陷的风险。

== 初始化顺序

内存管理子系统的初始化遵循严格的从底至上顺序，这个顺序由各层之间的依赖关系决定。首先，架构层构造启动上下文，提供物理内存范围和页表操作回调。然后，早期内存分配器从启动上下文获取物理内存范围，建立线性分配能力。接着，伙伴分配器从早期内存分配器接管所有物理页帧，建立运行期分配能力。虚拟地址空间管理器在伙伴分配器就绪后初始化，为内核堆分配虚拟地址区间。Slab 分配器和内核堆分配器在虚拟地址空间管理器就绪后初始化，提供小对象缓存和大对象分配能力。最后，进程地址空间管理器在所有下层就绪后初始化，为用户态进程提供地址空间管理。

这种严格的初始化顺序保证了每一层在初始化时，它所依赖的下层已经就绪。早期内存分配器不依赖任何其他分配器，伙伴分配器依赖早期内存分配器的移交，虚拟地址空间管理器依赖伙伴分配器的物理页帧供应，Slab 分配器依赖虚拟地址空间管理器的地址空间分配，进程地址空间管理器依赖所有下层和架构注入契约。这种严格的依赖链确保了初始化过程不会出现循环等待或使用未初始化的服务。

== 工程设计总结

内存管理子系统的设计围绕一个核心矛盾展开：内核对内存的需求从启动期到运行期跨越了数个数量级，而任何单一的分配策略都无法同时满足所有阶段的需求。早期内存分配器用最简陋的策略解决了"从无到有"的问题——在没有任何分配能力的情况下，线性递增指针提供了最原始的分配手段，使得后续的分配器可以借助它来构建自身。伙伴分配器用经典的分裂-合并算法解决了外部碎片和物理页帧管理的问题——通过将内存按 2 的幂次方组织，伙伴系统在分配效率和碎片控制之间取得了良好的平衡。虚拟地址空间管理器解决了虚拟地址与物理地址关联的问题——通过将虚拟地址的分配与物理页帧的映射解耦，它使得内核堆可以按需扩展，而不需要一次性映射整个虚拟地址范围。Slab 分配器解决了小对象高频分配的效率问题——通过对象缓存和空闲链表内嵌，它将小对象的分配开销降低到了接近指针操作的级别。进程地址空间管理器解决了用户态进程的地址空间隔离和按需分配问题——通过 VMA 集合、写时复制和共享页缓存，它在保证进程隔离性的同时最大限度地减少了不必要的物理页帧复制。

单向依赖关系是这个设计能够运转的根本保证。信息从底层流向高层，从物理层流向虚拟层，从内核态流向用户态。上层依赖下层提供的服务，但下层不知道上层的存在。这种单向依赖使得每一层都可以独立演化和优化——改进 Slab 分配器的缓存策略不会影响伙伴分配器的实现，优化缺页处理路径不会影响 VMA 集合的管理逻辑，添加新的 VMA backing 类型不需要修改伙伴分配器的代码。修改某一层时，开发者只需理解该层的内部逻辑和它与相邻层的接口契约，而不需要通读整个内存管理子系统的代码。

架构注入契约模式使得整个进程地址空间管理可以跨架构复用。四个操作表将所有架构相关的细节封装在架构层内部，通用层通过函数指针调用这些操作，从而实现了平台无关的地址空间管理逻辑。这种封装的意义不仅在于代码复用——更重要的是，它将架构相关的知识集中到了架构层，使得通用层的开发者不需要理解每种处理器的页表格式和异常模型，而架构层的开发者只需要实现操作表中的函数，不需要理解上层的 VMA 管理和缺页处理逻辑。这种知识的隔离降低了开发的认知负担，也减少了跨层修改引入缺陷的风险。

安全边界的设计是内存管理子系统可靠性的基石。异常表机制将内核对用户态内存的安全访问从"先检查后访问"的模式转变为"乐观执行、异常修复"的模式，在正常路径上消除了安全检查的开销，在异常路径上提供了可靠的恢复手段。缺页处理程序对缺页类型的严格分类和判断，确保了不同类型的缺页都能得到正确的处理——匿名映射的缺页分配零页，文件映射的缺页从文件读取或使用缓存，栈区域的缺页扩展 VMA，写时复制的缺页复制页帧，权限违反的缺页投递信号。这种分类处理避免了误判导致的内核崩溃或安全漏洞，使得内存管理子系统在面对各种异常情况时都能保持稳定。

分层架构的另一个重要优势是"接口契约"的确立。每一层通过明确的接口与上下层交互，这种契约使得层内部可以在不影响其他层的情况下自由演进。例如，我们可以替换 Slab 分配器的实现算法而不影响伙伴系统或内核堆管理器；我们可以修改虚拟内存管理器的页表结构而不影响上层的地址空间抽象。这种模块化设计是大型系统长期可维护性的基础。

最后，内存管理子系统展现了操作系统设计中"效率与正确性的平衡"。过度追求效率可能导致复杂的实现和难以理解的语义；过度追求简化可能导致性能损失和资源浪费。本系统在两者之间找到了合理的平衡点：启动分配器极度简化以确保可靠；运行期分配器在保证正确性的前提下追求效率；虚拟内存管理在提供完整功能的同时保持简洁的接口；垃圾回收在自动化的同时允许显式控制的出口。这种平衡使得系统既能高效运行，又能保持清晰和可控。

从更宏观的角度来看，内存管理子系统的设计反映了一种普遍的软件工程哲学：分而治之。通过将一个复杂的问题分解为多个相对简单的子问题，每一部分都可以独立优化和发展。这种分解不是随意的划分，而是基于对问题本质的深刻理解：小对象分配与大对象分配有着根本不同的性能特征，物理内存管理与虚拟地址空间管理有着不同的约束条件。这些差异性正是分层设计的依据。
